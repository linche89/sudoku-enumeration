# Milestone — 数独终局计数：从枚举到组合计数

> **终极目标：** 用**纯组合计数**（不逐一枚举终局）算出
> $N_0 = 6\,670\,903\,752\,021\,072\,936\,960$，并把运行时间从基线的 **344 s** 推到 **亚秒级**。
>
> 每完成一个里程碑就把 `- [ ]` 改成 `- [x]`，并在对应行记录"验证闸门"是否通过 + 提交哈希。

---

## 🛡️ 安全网（每步都拿它们当裁判）

- **已知真值** `N0 = 6670903752021072936960`，`N1 = 18383222420692992`，`S = 3546146300288 = 2^7·27704267971`。
- **合法带总数** `948109639680 = 9!·56·6^6`（M4 的验证闸门）。
- **基线（master 分支）**：枚举法，71 类，逐类解数 `results2.txt`，已全部对账通过。
- **参考 oracle** `reference/equiv.c`（Ed Russell）：44 类 + 模板计数，编译运行通过，逐类解数见其文件头。

---

## 📦 参考代码盘点

- ✅ `reference/equiv.c` — 44 类归并 + 模板位运算计数（**可编译、可运行、可移植**）。
- ✅ `reference/sudoku_equiv.cc` — 71 类归并（基线已独立复现）。
- ⚠️ Pettersen 纯组合公式 $\sum B(g_1)B(g_2)B(g_3)$ 的原始代码：仅散见于 enjoysudoku 论坛，难以干净获取 → **M4/M5 自行推导实现**。

---

## 🚩 里程碑

### 阶段 〇：起点（已完成）
- [x] **M0 — 枚举法基线** ：71 类 + 多线程位运算 DFS，结果 = N₀，344 s。`commit 992a4f5`（master）
- [x] **M1 — git + 调研(P0)** ：建仓、`combinatorial-counting` 分支、摸清 Pettersen band-generator 方法与 `equiv.c` 的算法。

### 阶段 一：吸收 44 类 + 模板法（低风险、大提速）
- [x] **M2 — 独立复现 71→44 归并** ✅
      `src/reduce44.cpp`：36288 带 → 列签名（22266 个）→ 对称着色（relabel + 宫序 + 列/行置换）→ **44** 类。
      *闸门通过：* 我的 44 类 `idx:rep:mult` 表与 `reference/equiv.c` **逐字节一致**（44/44）；Σ(mult·sol)=S=3,546,146,300,288 → N₀ 精确；44 个解数恰好 = 基线 71 类的 44 个互异值。
- [ ] **M3 — 模板法计数器（fast enumeration）**
      移植"放完 1 后，用 `uint32` 位模板枚举数字 2–9"的计数核（`equiv.c::solve_rep` level≥22），替换基线的逐格 DFS。
      *闸门：* 逐类解数 == oracle；总计 == N₀；运行时间 **≪ 344 s**（目标 < 10 s）。

### 阶段 二：纯组合计数（真正 push the boundary）
- [x] **M4 — 带签名计数 $B(\sigma)$** ✅
      `src/combinatorial.cpp`：$B(\sigma)=\sum_{	ext{row0 transversal}}2^{\#	ext{cycles}}$（残差 2-正则图的 2-染色）。
      *闸门通过：* 固定 box1 规范型，$\sum_{	ext{box2,box3}}B=564\,350\,976$（=948,109,639,680/1680）✅；每盒兼容数 $|compat|=56$。
- [x] **M5 — 组合计数补全数 $C(\sigma)$ + 聚合到 N₀** ✅
      `src/combinatorial2.cpp`：$C(\sigma^1)=\sum_{(\sigma^2,\sigma^3)	ext{划分}}B(\sigma^2)B(\sigma^3)$，每盒 56 个兼容划分。
      *闸门通过：* 44/44 类的 $C_i=72\cdot	ext{solcount}_i$ 全部吻合；$N_0=9!\cdot72\cdot\sum_i 	ext{mult}_i C_i=6{,}670{,}903{,}752{,}021{,}072{,}936{,}960$ 精确（**全程零终局枚举**）。运行 ~60 s。
- [x] **M6 — 推到亚秒级** ✅
      关键优化：列签名塌缩为 280 个"形状" → $B$ 退化为稠密 280×280 查表（消除全部哈希）；relabel 规范化预计算成 `rel` 表；锚点只取 `compat(P)∪compl(P)`。
      *闸门通过：* 结果仍 = N₀ 精确（44/44）；单线程 **~0.9 s**（核心组合求和仅 ~0.03 s，其余为一次性建表）——相对基线 344 s 约 **380×**。

### 阶段 三：收尾
- [ ] **M7 — 文档与对比**
      在 `FJ_sudoku.md` 增补"组合计数"章：方法推导、$B(\sigma)$、兼容求和、44 类、三法（枚举/模板/纯组合）性能对比表，并交叉验证。
- [ ] **M8 — 合并回 master**
      所有闸门通过后，把 `combinatorial-counting` 合并入 `master`，打 tag。

---

## 📊 进度

| 里程碑 | 状态 | 验证闸门 | commit |
|---|:--:|---|---|
| M0 基线枚举 | ✅ | N₀ ✓ / 344s | 992a4f5 |
| M1 git+调研 | ✅ | — | — |
| M2 44 类归并 | ✅ | == equiv.c 44 表 (44/44) + N₀ | reduce44 |
| M3 模板计数 | ⬜ | N₀ ✓ / <10s | — |
| M4 $B(\sigma)$ | ✅ | Σ=564,350,976×1680 | combinatorial |
| M5 组合 C+N₀ | ✅ | 44/44 C_i + N₀ ✓ / ~60s | combinatorial2 |
| M6 亚秒级 | ✅ | N₀ ✓ / ~0.9s (核心 0.03s) | combinatorial2 |
| M7 文档 | ⬜ | 三法对账 | — |
| M8 合并 | ⬜ | — | — |

---

*这是一场从"算得对"到"算得巧"的旅程：先把 70 亿次枚举（344 s）压成模板法的秒级，再用纯组合公式把它彻底解构成与终局总数无关的常数级求和。Let's go. 🚀*
