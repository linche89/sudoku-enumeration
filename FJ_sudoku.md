# 数独终局总数的精确枚举：Felgenhauer–Jarvis (2005) 算法本地复现

> **复现目标数：** $N_0 = 6\,670\,903\,752\,021\,072\,936\,960 \approx 6.671\times10^{21}$
>
> **本仓库以现代 C++20 从零重写了 FJ05 全流水线**，独立产出与原始论文 / 原始程序逐字节一致的 71 个等价类，并以多线程位运算 DFS 完成 54 亿余次终局枚举，最终聚合得到上述 22 位精确总数——与 1975 年以来悬而未决、2005 年由 Bertram Felgenhauer 与 Frazer Jarvis 首次给出的权威结果完全吻合。

---

## 目录

1. [问题与结论](#1-问题与结论)
2. [数学背景：为什么不能"直接数"](#2-数学背景为什么不能直接数)
3. [FJ05 的核心思想：群论降维 + 暴力计数](#3-fj05-的核心思想群论降维--暴力计数)
4. [五阶段计算流水线](#4-五阶段计算流水线)
5. [阶段 1–3：从 2 612 736 个顶带到 71 个等价类](#5-阶段-13从-2-612-736-个顶带到-71-个等价类)
6. [阶段 4：O(1) 位掩码并行 DFS](#6-阶段-4o1-位掩码并行-dfs)
7. [阶段 5：大数聚合与因子分解](#7-阶段-5大数聚合与因子分解)
8. [工程实现与代码层级](#8-工程实现与代码层级)
9. [性能工程：本复现中真正的瓶颈在哪里](#9-性能工程本复现中真正的瓶颈在哪里)
10. [多层验证](#10-多层验证)
11. [完整数据：71 个等价类](#11-完整数据71-个等价类)
12. [洞察与延伸](#12-洞察与延伸)
13. [如何构建与运行](#13-如何构建与运行)
14. [参考文献](#14-参考文献)

---

> **第二部分（续章）：组合计数 — 从"算得对"到"算得巧"**
> 第一部分用对称性降维 + 暴力 DFS 枚举了约 70 亿个终局（344 s）。第二部分换一种思路——**不枚举任何一个完整终局**，把 $N_0$ 直接组合算出来，最终单线程 **~0.9 s**。
> 15. [核心因子化](#15-核心因子化三带列签名) · 16. [B(σ) 与 2^环](#16-bσ-与-2环) · 17. [列兼容与 C(σ)](#17-列兼容与-cσ) · 18. [桥梁与亚秒优化](#18-桥梁与亚秒优化) · 19. [三法对比与交叉验证](#19-三法对比与交叉验证) · 20. [尾声：彻底自洽](#20-尾声彻底自洽44-类只是优化而非必需)

---

## 1. 问题与结论

一个标准数独终局是一个 $9\times9$ 的方阵，每行、每列、每个 $3\times3$ 宫都恰好是 $1$–$9$ 的一个排列。问题是：**这样的方阵一共有多少个？**

这个看似简单的计数问题没有已知的简单组合闭式解（它是拉丁方计数的特例，而拉丁方计数本身就是公认的难题）。2005 年 6 月，Felgenhauer 与 Jarvis 用"对称性降维 + 计算机暴力枚举"给出了精确答案：

$$\boxed{N_0 = 6\,670\,903\,752\,021\,072\,936\,960}$$

本复现工程**重新独立完成了全部计算步骤**，并在每一步与原始产物对拍，最终复现出该数。下表是程序 `fj_sudoku.exe` 的实际输出（关键字段）：

```text
================ FJ05 Sudoku enumeration ================
equivalence classes    : 71      (expected 71)        [OK]
sum of multiplicities  : 36288   (expected 36288)     [OK]
S  = sum(mult*sol)     : 3546146300288                [OK]
N1 = 72^2 * S          : 18383222420692992            [OK]
N0 = 9!  * N1          : 6670903752021072936960       [OK]
cross-check vs jobs2.txt   : 71/71 multiplicities match
cross-check vs results2.txt: 71/71 solution counts match
RESULT: ALL CHECKS PASSED
```

---

## 2. 数学背景：为什么不能"直接数"

朴素地"对每个格子试 1–9、回溯"需要遍历的状态空间约为 $9^{81}\approx 2\times10^{77}$，即便用约束剪枝，合法终局也有 $6.67\times10^{21}$ 个——**逐一枚举每一个终局在物理上不可行**（按每秒枚举 $10^{10}$ 个算，需要约 $2\times10^4$ 年）。

参照系：

| 对象 | 数量 |
|:--|--:|
| $9\times9$ 拉丁方 | $5\,524\,751\,496\,156\,892\,842\,531\,225\,600 \approx 5.525\times10^{27}$ |
| **数独终局（拉丁方的子集）** | $\mathbf{6\,670\,903\,752\,021\,072\,936\,960 \approx 6.671\times10^{21}}$ |
| 占拉丁方比例 | $\approx 1.2\times10^{-6}$ |

数独多出了"宫"约束，使合法解只占拉丁方的百万分之一。FJ 的关键贡献，是把这个 $6.67\times10^{21}$ 规模的枚举，通过群论降维压缩到**仅需对 71 个代表性局面**各做一次约 $10^8$ 次的暴力搜索。

---

## 3. FJ05 的核心思想：群论降维 + 暴力计数

数独终局在一组**保持解数不变的对称操作**下构成等价类。这些操作（"数独对称群"的生成元）包括：

- **重贴标签（Relabelling）**：把数字 $1$–$9$ 做任意置换（$9!$ 种）；
- **宫内行/列置换**：在同一带（band，行块）内交换三行、在同一栈（stack，列块）内交换三列；
- **行块/列块置换**：交换三条带、交换三条栈；
- **转置**。

只要两个局面能通过这些操作互相变换，它们补全成完整终局的方式数就**完全相同**。FJ 的策略：

1. **把左上角宫 B1 固定为标准序** `123/456/789`——任何终局都可通过重贴标签唯一地化为此形，因此

$$N_0 = 9!\cdot N_1,\qquad N_1 = \#\{\text{B1 为标准序的终局}\}.$$

2. **只枚举顶带（B1,B2,B3，即前三行）的所有合法填法**，对每种填法，整张终局的补全数仅取决于该顶带（在对称意义下）；
3. **把 2 612 736 种顶带按"补全数相等"归并为 71 个等价类**，每类记录其轨道大小 $S_i$；
4. **对 71 个代表各做一次暴力 DFS**，得到补全数 $c_i$；
5. **聚合** $N_0 = 9!\cdot 72^2\cdot\sum_i S_i c_i$。

> 九宫格编号约定（贯穿全文）：
>
> ```text
>        col-block 0   col-block 1   col-block 2
>       +-----------+-------------+-------------+
> band0 |    B1     |     B2      |     B3      |  行 0..2  ← "顶带 / Top Band"
>       +-----------+-------------+-------------+
> band1 |    B4     |     B5      |     B6      |  行 3..5  ← "中带"
>       +-----------+-------------+-------------+
> band2 |    B7     |     B8      |     B9      |  行 6..8  ← "底带"
>       +-----------+-------------+-------------+
> ```

---

## 4. 五阶段计算流水线

```text
+================================================================================+
|                  SUDOKU ENUMERATION PIPELINE (FJ05 MODEL)                      |
+================================================================================+
| PHASE 1  冻结 B1 = 123/456/789                          全局乘子  9! = 362 880 |
+--------------------------------------------------------------------------------+
| PHASE 2  枚举合法顶带 (B2,B3)                            56 × 6^6 = 2 612 736   |
+--------------------------------------------------------------------------------+
| PHASE 3  对称降维                                                              |
|   (a) 字典序约简：栈内列置换 + 交换 B2/B3   ÷72  →  36 288                      |
|   (b) 精化等价（重贴标签 + 行/列/宫置换 + 2×k 矩形重标）→  71 个等价类          |
+--------------------------------------------------------------------------------+
| PHASE 4  并行位运算 DFS                                                        |
|   对 71 个代表，各固定 B1+B2+B3 与 B4/B7 首列(÷72)，回溯填满剩余 48 格          |
|   得到每类补全数 c_i（已含 9!·72·72 的约简）                                    |
+--------------------------------------------------------------------------------+
| PHASE 5  全局聚合（u128）                                                      |
|   S  = Σ S_i·c_i = 3 546 146 300 288                                           |
|   N1 = 72² · S   = 18 383 222 420 692 992                                      |
|   N0 = 9!  · N1  = 6 670 903 752 021 072 936 960                               |
+================================================================================+
```

降维因子的来源（**全局约简因子 $=9!\cdot72^2 = 1\,881\,169\,920$**）：

| 因子 | 来源 | 作用对象 |
|:--|:--|:--|
| $9!=362880$ | 重贴标签固定 B1 | 整张终局 |
| $72 = 3!\cdot3!\cdot2$ | B2 内列置换 · B3 内列置换 · 交换 B2/B3 | 顶带（$2\,612\,736\to36\,288$） |
| $72 = 3!\cdot3!\cdot2$ | B4 内行置换 · B7 内行置换 · 交换 B4/B7 | 左列块首列（DFS 内的 `rem` 表，10 种规范型） |

---

## 5. 阶段 1–3：从 2 612 736 个顶带到 71 个等价类

### 5.1 顶带计数 $56\times6^6 = 2\,612\,736$

B1 固定后，考察 B2 的首行。它要么完全取自 B1 第 2 行 `{4,5,6}` 或第 3 行 `{7,8,9}`（"纯 pure"），要么是两者的混合（"混 mixed"）。穷举可得 **2 种纯首行 + 18 种混首行**：

- **纯**首行（如 `{4,5,6}|{7,8,9}`）：下两行被强约束，给出 $(3!)^6$ 种填法；
- **混**首行（如 `{4,5,7}|{6,8,9}`）：自由度更高，给出 $3\cdot(3!)^6$ 种。

$$2\cdot(3!)^6 + 18\cdot3\cdot(3!)^6 = 56\cdot(3!)^6 = 56\times46656 = 2\,612\,736.$$

### 5.2 字典序约简 $\div72 \to 36\,288$

对每个顶带：① 把 B2、B3 各自的三列按首行元素升序排列（各 $3!=6$ 种，共 $36$）；② 若 B2 字典序大于 B3 则交换（$\times2$）。这 $72$ 种操作不改变补全数，于是

$$2\,612\,736 / 72 = 36\,288.$$

代表态可只用 B2,B3 的内容（每行 6 个数字）表示，例如 `[456789,789123,123456]`。

### 5.3 精化等价 $36\,288 \to 71$

仅用 5.2 仍需对 36 288 个局面各做暴力计数（耗时数十小时），不可接受。FJ 进一步利用更多保解数操作，构造其**自反–对称–传递闭包**，用并查集归并。本复现实现的生成元与逐步类数（**与原始 `sudoku_equiv.cc` 注释逐一吻合，已实测验证**）：

```text
        操作生成元                        闭包后剩余类数
  ─────────────────────────────────────  ───────────────
  起点（字典序约简后）                          36 288
  R  交换顶带的两行 (+ 重标回标准)               6 240
  C  交换 B1 的两列 (+ 重标回标准)               1 089
  B  交换两条栈/宫 (+ 重标回标准)                  416
  2×2 矩形重标（对角同值的 2 列）                  174
  2×3 矩形重标（3 列上数字成 3-环）                141
  3×2 矩形重标（每栈各 1 列、2 行成 3-环）           86
  4×2 / 一般 2×k 行子集重标                        71  ← 最终
```

**矩形重标**是 FJ 的精妙之处。例如下列顶带中，数字 1 与 4 在 B1 的某列以 `(1,4)` 出现，又在 B2/B3 的某列以相反顺序 `(4,1)` 出现：

```text
   1 2 3 | 4 5 8 | 6 7 9        ←  列0 的 (1,4) 与 列3 的 (4,1) 配对
   4 5 6 | 1 7 9 | 2 3 8
   7 8 9 | 2 3 6 | 1 4 5
```

把"另一处"的 1、4 互换得到的新顶带，其补全数与原顶带**相同**。一般地，若两行在某列子集 $S$ 上含有**相同的数字集合**，则交换这两行在 $S$ 上的元素是若干不相交循环重标的复合，因而保解数。

> **实现注记（一个值得记录的取舍）：** 原始 `sudoku_equiv.cc` 的 4×2 循环存在一处笔误（`for (int x4=x3+1; x3<9; x3++)` 中递增的是 `x3` 而非 `x4`），它"恰好"也归并到了 71 类。本复现没有照搬这个 bug，而是实现了**数学上干净的"一般行子集重标"**（即原作者注释中标注为 `if(0)` 的通用版本：对任意列子集 $S$，若两行在 $S$ 上数字集合相同则交换）。两者闭包相同，本复现产出的 71 个 `(mult, 代表)` 与 `jobs2.txt` **逐字节一致**（见 §10）。

降维成效：$2\,612\,736 \to 36\,288 \to 71$，把外层循环压缩了 **36 800 倍**。

---

## 6. 阶段 4：O(1) 位掩码并行 DFS

### 6.1 状态用三组 9-bit 掩码表示

对每个代表，先固定：B1（标准序）、B2/B3（代表内容）、以及 **B4/B7 的首列**（列 0 的下 6 格，取 `rem` 表的 10 种规范型之一——这正是 5.1 中"左列块"方向的 $\div72$ 约简）。随后回溯填满**剩余 48 格（行 3–8 × 列 1–8）**。

每行、每列、每宫各用一个 `uint16_t` 记录"已用数字"的位集（bit $i$ 置位 = 数字 $i{+}1$ 已用）：

```text
  bit:     8 7 6 5 4 3 2 1 0
  digit:   9 8 7 6 5 4 3 2 1

  行掩码 | 列掩码 | 宫掩码   →  OR   = 已被禁用的数字
  可用候选 = (~已用) & 0x1FF        ← 0x1FF = 9 个 1，即数字 1..9
```

热点循环用三条无分支位技巧逐一取候选：

```cpp
unsigned avail = 0x1FFu & ~(row[r] | col[c] | box[b]);   // 候选集合
while (avail) {
    unsigned bit = avail & (0u - avail);   // 取最低位的 1   (BLSI / x & -x)
    avail -= bit;                          // 清除该位       (avail &= avail-1)
    row[r] |= bit; col[c] |= bit; box[b] |= bit;
    cnt += dfs<K+1>(...);                  // 递归
    row[r] ^= bit; col[c] ^= bit; box[b] ^= bit;
}
```

合法性检查是 **O(1)**（三次按位或 + 一次按位与非），完全不出现数组遍历或 `if-else` 分支，正是 plan 所要求的设计。

### 6.2 模板展开的固定填格顺序

48 个待填格的访问顺序在**编译期**用 `constexpr` 固定为**行优先（行 3→8，列 1→8）**；DFS 用 `template<int K>` 在编译期递归展开，使每一层的 `(行,列,宫)` 下标都是字面常量，整条 48 层链内联成近乎无分支的紧致代码。选择行优先的原因见 §9（它把搜索树规模压到反对角线序的约一半）。

### 6.3 并行拓扑：静态工作队列

把 $71\times10 = 710$ 个 `(类, rem)` 子任务放入一个原子计数器队列，由 24 个工作线程抢占式领取；每线程持有**独占的 64 字节对齐状态**（避免伪共享），各自累加到线程本地的 `sol[71]`，最终归并：

```text
              Master: 710 atomic tasks  (71 classes × 10 rem)
                          | fetch_add(1)
   +-------------+--------+--------+-------------+
   | Worker 1    | Worker 2 |  ...  | Worker 24  |   每个 alignas(64) State
   | local[71]   | local[71]|       | local[71]  |   独占一条 cache line
   +------+------+----+-----+-------+------+------+
          |           |             |
          +-----------+------+------+
                             | reduce
                  sol[i] = Σ_threads local[i]
```

---

## 7. 阶段 5：大数聚合与因子分解

设第 $i$ 个代表的补全数为 $c_i$（程序内变量 `sol_i`，已是"约简后"的计数），轨道大小为 $S_i$（`mult_i`，$\sum S_i = 36\,288$）。则：

$$
S \;=\; \sum_{i=1}^{71} S_i\,c_i \;=\; 3\,546\,146\,300\,288
$$
$$
N_1 \;=\; 72^2\cdot S \;=\; 18\,383\,222\,420\,692\,992 \qquad(\text{B1 为标准序的终局数})
$$
$$
N_0 \;=\; 9!\cdot N_1 \;=\; 6\,670\,903\,752\,021\,072\,936\,960
$$

> $N_0$ 超过 $2^{63}$（`uint64_t` 上限 $\approx 1.8\times10^{19}$），因此最后一步乘法用 **`unsigned __int128`**，并手写十进制转换输出 22 位精确值，杜绝任何浮点截断。

**因子分解**（与论文一致）：

$$S = 2^{7}\cdot 27\,704\,267\,971,\qquad 27\,704\,267\,971\ \text{为素数}$$
$$N_0 = 2^{20}\cdot 3^{8}\cdot 5\cdot 7\cdot 27\,704\,267\,971$$

那个 11 位素数 $27\,704\,267\,971$ 同时出现在 $S$ 与 $N_0$ 中，是数独终局总数"不可约的算术内核"——这也直观说明了为何不存在简单的组合闭式公式。

---

## 8. 工程实现与代码层级

```text
sudoku_FJ/
├── plan.md                  规格说明（输入）
├── FJ_sudoku.md             本复现文档
├── src/
│   ├── band.hpp             顶带表示 + 规范化（normalize / key / pretty）
│   ├── reduce.hpp           Phase 2+3：generate() 生成 36288；reduce() 并查集归并到 71
│   ├── count.hpp            Phase 4+5：constexpr 填格序 + 模板位运算 DFS + 多线程聚合(u128)
│   ├── main.cpp             驱动 + 多层自检 + 报告
│   ├── reduce_main.cpp      独立的"只输出 71 类作业表"工具（用于对拍 jobs2.txt）
│   └── bench.cpp            单类计时探针
├── reference/               FJ 原始程序与数据（作为"金标准"对照，非计算引擎）
│   ├── sudoku_equiv.cc  sudoku2.cc  sudoku_verify.py  equiv.c  sudoku.hs
│   └── jobs2.txt  results2.txt  sudoku.pdf
├── build/                   可执行文件
└── data/                    运行输出与对拍数据
```

**数据流（无堆分配的热点路径）：** `reduce()` 一次性算出 71 个 `Class{mult, repr, band}` → `count_all()` 为每类预计算 B1+B2+B3 的基掩码 → 工作线程对 `(类, rem)` 调用 `count_branch()` → 模板 DFS `dfs<K>` 在栈上的 `State{uint16_t row[9],col[9],box[9]}`（扁平定长数组，DFS 核心**零 `std::vector`/`Vec` 堆分配**）上计数 → 归并 → `__int128` 聚合。

设计要点对应 plan 的验收标准：

| plan 验收项 | 本实现 |
|:--|:--|
| DFS 核心无堆分配，状态为定长 `[u16;9]` | `struct State{uint16_t row[9],col[9],box[9];}`，栈上分配 |
| O(1) 位掩码验证 + LSB 技巧 + 硬件指令 | `avail & -avail`(BLSI)、`avail &= avail-1`、`-mpopcnt/-mbmi` |
| 模板常量折叠 | `template<int K> dfs` + `constexpr Order` |
| 71 类等价数据源接口 | `reduce()` 在程序内重算（不外部载入），并可 `--emit-jobs` 导出 |
| 大数：u64 中继、u128 终值 | `S,N1` 为 `uint64_t`；`N0` 为 `unsigned __int128` |
| Work-stealing / 静态分发 + 避免伪共享 | `std::atomic` 任务队列 + `alignas(64)` 线程状态 |

---

## 9. 性能工程：本复现中真正的瓶颈在哪里

> "为什么不直接用 reference 里的原版代码？" —— 这是复现过程中最关键的一个工程问题，下面用实测数据回答。

### 9.1 原版代码并不"高效"，且无法直接编译

- **编译失败**：`sudoku2.cc` 用了 `template<> static inline void search<8,9>()`，现代 C++ 禁止显式特化带存储类，g++ 13 直接报错（需打补丁）。
- **运行慢**：把打补丁后的原版 `sudoku2.cc` 用 `-O3` 在本机（12 物理核 / 24 逻辑）运行，**单个 config 0 就 >120 秒未跑完**（其每 100 万解刷新一次的进度计数器证实了它确实在缓慢推进）。原版是**单线程、一个进程只算一个 config**、靠 shell 脚本跑 71 次的结构，全部跑完单线程约 **2–2.5 小时**（恰好对应论文中 2005 年"a few hours"）。

结论：原版是极好的**对照标准答案**（本复现确实编译并运行了 `sudoku_equiv.cc` 来校验降维），但它不是一个"高效计算引擎"。瓶颈在**算法（填格顺序）与单线程**，而非"代码老旧"。

### 9.2 搜索树规模是第一性瓶颈，而非单节点速度

待枚举的终局总数（即所有叶子数）$\sum_i c_i = 6\,956\,463\,104 \approx 7\times10^9$ 是**不可压缩的工作量**。真正决定耗时的是"**内部节点 / 叶子**"之比，它完全由填格顺序决定。本复现实测（config 0，单分支，6 696 000 个叶子）：

| 填格策略 | 内部节点/叶子 | 单节点吞吐 | 等效叶子吞吐 | 评价 |
|:--|--:|--:|--:|:--|
| 反对角线（原版 FJ 序） | **62.8** | ~92 M/s | 1.4 M/s | 节点便宜但树太臃肿 |
| **行优先（本复现采用）** | **33.0** | ~64 M/s | **1.9 M/s** | 最优折中 ✓ |
| 列优先 | 46.6 | ~90 M/s | 1.9 M/s | — |
| 动态 MRV（最少候选优先） | **13.3** | ~12 M/s | 0.9 M/s | 树最小，但每节点 O(48) 扫描太贵 |

**关键洞察：** MRV 把搜索树压到反对角线的 1/5，却因每个节点要扫描候选数而让单节点吞吐暴跌 8 倍，**净效果反而更慢**。在这个问题上，"便宜的固定顺序节点"胜过"昂贵的智能节点"——行优先固定序以约 $1.9$ M 叶子/秒/核成为最佳实用选择。

### 9.3 一个 Windows/MinGW 实战陷阱：`-march=native` 触发 AVX 栈对齐崩溃

最初用 `-O3 -march=native` 编译会**段错误**（崩在 `reduce()` 内 `std::unordered_map` 的 AVX 指令上）。这是 MinGW-GCC 在 Windows 上的著名问题：`-march=native` 启用 AVX，其 32 字节对齐的访存与 Windows 仅保证 16 字节的栈对齐冲突。本问题的 DFS 是纯标量位运算，AVX 毫无收益。**修复：去掉 `-march=native`，改用标量位操作指令集** `-mtune=native -mbmi -mbmi2 -mpopcnt -mlzcnt`（恰好提供 plan 想要的 `BLSI`/`TZCNT`/`POPCNT`）。

### 9.4 实测总耗时

得益于"更优填格序 + 24 线程 + 现代硬件"，本复现把原版的小时级单线程计算压缩到约 **5.7 分钟**：

```text
wall-clock time        : 343.945 s   (24 threads, 12 物理核)
RESULT: ALL CHECKS PASSED
```

| 指标 | 数值 |
|:--|--:|
| 枚举的终局总数（叶子） | $6\,956\,463\,104 \approx 7.0\times10^9$ |
| 墙钟时间（24 线程） | $343.9\ \text{s}\ (\approx 5.7\ \text{min})$ |
| 聚合吞吐 | $\approx 2.0\times10^7$ 叶子/秒 |
| 单核估算（行优先序） | $\approx 3\,660\ \text{s}$ |
| 实测并行加速比 | $\approx 10.6\times$（12 物理核 + 超线程，受内存带宽限制） |
| 相对 2005 年单核（约 2–2.5 h）的墙钟提速 | $\approx 20\text{–}25\times$ |

要再快一个数量级，需放弃"逐一枚举"改用"组合计数"（对中带枚举、底带用列剩余集签名做记忆化计数），或进一步把 71 类合并到 §12 提到的 44 类——两者都显著更复杂，超出本 plan 的"位运算 DFS"范围。

---

## 10. 多层验证

复现的可信度来自"独立重写 + 与多个金标准逐字节/逐值对拍 + 与历史独立验证一致"：

```text
┌─ 层 1：降维正确性 ───────────────────────────────────────────────┐
│  • 本复现 reduce() 输出 71 类 (mult, 代表)                        │
│  • 与原版 sudoku_equiv.cc 现编现跑的输出 → 逐字节一致            │
│  • 与官方 jobs2.txt                     → 逐字节一致 (71/71)     │
│  • Σ mult = 36288 ✓                                              │
└──────────────────────────────────────────────────────────────────┘
┌─ 层 2：计数正确性 ───────────────────────────────────────────────┐
│  • 本复现 71 个补全数 c_i                                        │
│  • 与官方 results2.txt 逐值对拍        → 71/71 完全一致          │
└──────────────────────────────────────────────────────────────────┘
┌─ 层 3：聚合正确性 ───────────────────────────────────────────────┐
│  • S=3 546 146 300 288, N1, N0 与已知权威值逐位一致              │
└──────────────────────────────────────────────────────────────────┘
┌─ 层 4：历史独立验证（文献佐证）─────────────────────────────────┐
│  • Ed Russell 用完全不同的算法（先放所有 1，再放所有 2…）独立    │
│    复算，得到相同结果，且快 3 倍                                 │
│  • 原作者用专门写的 sudoku_verify.py 验证了 71 类归并森林        │
│  • Kevin Kilfoil 的启发式近似与精确值仅差 0.2%                   │
└──────────────────────────────────────────────────────────────────┘
```

程序内置自检：`fj_sudoku.exe` 在打印 $N_0$ 后，自动与 `reference/jobs2.txt`、`reference/results2.txt` 及硬编码的已知 $N_1,N_0$ 对拍，仅当 **全部通过** 才返回 `RESULT: ALL CHECKS PASSED`（退出码 0）。

---

## 11. 完整数据：71 个等价类

下表为全部 71 个等价类的代表（B2,B3 的内容）、轨道大小 $S_i$、补全数 $c_i$ 及其乘积贡献 $S_i c_i$。**本复现的 $S_i$ 与 $c_i$ 与官方 `jobs2.txt`/`results2.txt` 全部 71 行逐一吻合。**

| # | 代表 (B2,B3) | 轨道 $S_i$ | 补全数 $c_i$ | $S_i\cdot c_i$ |
|--:|:--|--:|--:|--:|
| 1 | `456789,789123,123456` | 4 | 108,374,976 | 433,499,904 |
| 2 | `456789,789123,123465` | 108 | 102,543,168 | 11,074,662,144 |
| 3 | `456789,789123,123564` | 72 | 100,231,616 | 7,216,676,352 |
| 4 | `456789,789123,132465` | 216 | 102,047,904 | 22,042,347,264 |
| 5 | `456789,789123,132546` | 216 | 98,875,264 | 21,357,057,024 |
| 6 | `456789,789123,132564` | 2,592 | 98,733,568 | 255,917,408,256 |
| 7 | `456789,789123,231564` | 72 | 97,685,328 | 7,033,343,616 |
| 8 | `456789,789123,231645` | 510 | 98,950,072 | 50,464,536,720 |
| 9 | `456789,789132,123546` | 216 | 99,083,712 | 21,402,081,792 |
| 10 | `456789,789132,132546` | 216 | 98,875,264 | 21,357,057,024 |
| 11 | `456789,789132,213456` | 216 | 99,525,184 | 21,497,439,744 |
| 12 | `456789,789132,213546` | 1,296 | 98,369,440 | 127,486,794,240 |
| 13 | `456789,789132,213645` | 1,404 | 97,287,008 | 136,590,959,232 |
| 14 | `456789,789132,213654` | 540 | 98,128,064 | 52,989,154,560 |
| 15 | `456789,789132,231546` | 702 | 98,153,104 | 68,903,479,008 |
| 16 | `456789,789132,231564` | 270 | 97,961,464 | 26,449,595,280 |
| 17 | `456789,789132,231645` | 2,214 | 97,961,464 | 216,886,681,296 |
| 18 | `456789,789231,123645` | 24 | 97,282,720 | 2,334,785,280 |
| 19 | `456789,789231,132546` | 108 | 98,128,064 | 10,597,830,912 |
| 20 | `456789,789231,231564` | 6 | 98,950,072 | 593,700,432 |
| 21 | `456789,789231,231645` | 504 | 97,685,328 | 49,233,405,312 |
| 22 | `456789,789231,312456` | 6 | 99,258,880 | 595,553,280 |
| 23 | `456789,798132,213546` | 1,350 | 97,477,096 | 131,594,079,600 |
| 24 | `456789,798132,213645` | 666 | 97,549,160 | 64,967,740,560 |
| 25 | `456789,798132,231546` | 756 | 96,702,240 | 73,106,893,440 |
| 26 | `456789,798213,213564` | 702 | 97,477,096 | 68,428,921,392 |
| 27 | `456789,798213,213654` | 18 | 97,549,160 | 1,755,884,880 |
| 28 | `456789,897312,312564` | 20 | 94,888,576 | 1,897,771,520 |
| 29 | `457689,189237,263145` | 2,268 | 97,116,296 | 220,259,759,328 |
| 30 | `457689,189237,263154` | 1,134 | 98,371,664 | 111,553,466,976 |
| 31 | `457689,189237,263514` | 2,592 | 97,539,392 | 252,822,104,064 |
| 32 | `457689,189237,362145` | 2,052 | 96,482,296 | 197,981,671,392 |
| 33 | `457689,189237,362154` | 486 | 97,346,960 | 47,310,622,560 |
| 34 | `457689,189273,263145` | 972 | 97,910,032 | 95,168,551,104 |
| 35 | `457689,189273,263154` | 270 | 98,493,856 | 26,593,341,120 |
| 36 | `457689,189273,263514` | 864 | 98,119,872 | 84,775,569,408 |
| 37 | `457689,189273,362154` | 270 | 97,346,960 | 26,283,679,200 |
| 38 | `457689,189273,623154` | 864 | 98,733,184 | 85,305,470,976 |
| 39 | `457689,189273,632154` | 540 | 97,455,648 | 52,626,049,920 |
| 40 | `457689,189273,632514` | 972 | 97,116,296 | 94,397,039,712 |
| 41 | `457689,189327,362145` | 1,620 | 97,416,016 | 157,813,945,920 |
| 42 | `457689,189723,623145` | 432 | 98,784,768 | 42,675,019,776 |
| 43 | `457689,189723,623154` | 144 | 101,131,392 | 14,562,920,448 |
| 44 | `457689,189723,623514` | 432 | 97,992,064 | 42,332,571,648 |
| 45 | `457689,189723,632154` | 864 | 97,756,224 | 84,461,377,536 |
| 46 | `457689,189732,623514` | 324 | 96,380,896 | 31,227,410,304 |
| 47 | `457689,198723,236514` | 540 | 97,910,032 | 52,871,417,280 |
| 48 | `457689,198723,326514` | 270 | 97,416,016 | 26,302,324,320 |
| 49 | `457689,198723,632514` | 270 | 97,714,592 | 26,382,939,840 |
| 50 | `457689,198732,632514` | 234 | 96,100,688 | 22,487,560,992 |
| 51 | `457689,289137,361245` | 540 | 96,482,296 | 52,100,439,840 |
| 52 | `457689,289137,361254` | 288 | 96,807,424 | 27,880,538,112 |
| 53 | `457689,289137,631524` | 288 | 96,631,520 | 27,829,877,760 |
| 54 | `457689,289173,163452` | 6 | 99,258,880 | 595,553,280 |
| 55 | `457689,289173,613245` | 216 | 97,372,400 | 21,032,438,400 |
| 56 | `457689,289173,613254` | 54 | 97,714,592 | 5,276,587,968 |
| 57 | `457689,289731,163245` | 18 | 96,100,688 | 1,729,812,384 |
| 58 | `457689,289731,613254` | 108 | 97,372,400 | 10,516,219,200 |
| 59 | `457689,289731,613542` | 108 | 95,596,592 | 10,324,431,936 |
| 60 | `457689,298137,316245` | 54 | 95,596,592 | 5,162,215,968 |
| 61 | `457689,298137,316254` | 378 | 95,596,592 | 36,135,511,776 |
| 62 | `457689,298713,316245` | 108 | 96,482,296 | 10,420,087,968 |
| 63 | `457689,389271,612534` | 108 | 96,482,296 | 10,420,087,968 |
| 64 | `457689,389712,612345` | 54 | 98,371,664 | 5,312,069,856 |
| 65 | `457689,398217,126345` | 108 | 97,455,648 | 10,525,209,984 |
| 66 | `457689,398217,612345` | 36 | 97,372,400 | 3,505,406,400 |
| 67 | `457689,398721,126345` | 54 | 97,416,016 | 5,260,464,864 |
| 68 | `457689,398721,216354` | 108 | 97,287,008 | 10,506,996,864 |
| 69 | `457689,398721,612345` | 108 | 98,048,704 | 10,589,260,032 |
| 70 | `457689,891723,236514` | 54 | 98,493,856 | 5,318,668,224 |
| 71 | `457689,893271,216354` | 54 | 98,153,104 | 5,300,267,616 |
| | **合计** | **36,288** | — | **3,546,146,300,288** |

**数据透视：**

- $\sum_i S_i = 36\,288$（字典序约简后的局面总数），$\sum_i S_i c_i = S = 3\,546\,146\,300\,288$。
- 补全数 $c_i$ 范围：$\min = 94\,888\,576$（类 #28），$\max = 108\,374\,976$（类 #1）。极差仅约 14%——**所有顶带的补全数惊人地接近**，这正是 Kilfoil 启发式（假设各顶带补全数近似相等）能逼近到 0.2% 的原因。
- 轨道大小 $S_i$ 有 35 个不同取值，从 4 到 2 592（$=72^2/2$）。最大单类贡献来自 #6 `[456789,789123,132564]`（$S_i=2592,\ S_ic_i\approx2.56\times10^{11}$）。
- **71 个补全数中只有 44 个互不相同**——这暗示 71 类其实还能再合并为 **44** 类。FJ 正是先暴力算出 71 个 $c_i$、发现只有 44 个不同值，才反推出尚存未实现的等价关系；Ed Russell 的程序随后确认并实现了到 44 类的归并。本复现也独立复算出"恰好 44 个不同的 $c_i$"，与该历史发现吻合。

---

## 12. 洞察与延伸

1. **降维而非蛮力，是可计算性的关键。** 直接枚举 $6.67\times10^{21}$ 不可行；FJ 通过 $9!\cdot72^2$ 的对称约简 + 36288→71 的精化等价，把外层循环压到 71，把问题变成"几小时"（2005）乃至"几分钟"（本复现）量级。

2. **"等价类"的强弱有层次。** 行/列/宫置换 + 重标只能到 416 类；FJ 额外发现的 **2×k 矩形重标** 才推进到 71；而真正的补全数等价是 **44** 类。每加一层更强的等价识别，类数单调下降——但识别它们需要越来越精巧的组合论证。

3. **本问题的瓶颈不是"位运算多快"，而是"搜索树多大"。** 实测表明：固定行优先序（树 33×、节点极廉）优于动态 MRV（树 13×、节点昂贵）。在"枚举所有解"（而非"找一个解"）的场景下，叶子总数不可压缩，单节点成本与树形之间的权衡才是性能核心。

4. **复现工程的真实摩擦。** 2005 年的代码在 2026 年既编不过（`template<> static inline`），又会因 MinGW 的 AVX 栈对齐 bug 崩溃。这些"非算法"问题往往比算法本身更耗时——本复现把它们一一记录，正是复现价值的一部分。

5. **后续可优化方向。** 若追求秒级：对中带（行 3–5）枚举、对底带（行 6–8）用"列剩余集签名 + 记忆化"做组合计数，可避免逐一枚举 70 亿叶子；或进一步把 71 类合并到 44 类，再省约 38%。

---

## 13. 如何构建与运行

环境：Windows 11，g++ 13.1.0 (MinGW-W64)，12 物理核 / 24 逻辑核。

```bash
# 1) 构建主程序（注意：用标量位指令集，切勿用 -march=native）
g++ -O3 -mtune=native -mbmi -mbmi2 -mpopcnt -mlzcnt -funroll-loops \
    -std=c++20 -pthread -Isrc src/main.cpp -o build/fj_sudoku.exe

# 2) 完整复现（默认 24 线程，自动与 reference/ 对拍并打印 N0）
./build/fj_sudoku.exe --threads 24

# 3) 仅导出 71 类作业表，与官方 jobs2.txt 对拍
./build/fj_sudoku.exe --emit-jobs > data/my_jobs.txt
diff <(grep -v '^#' data/my_jobs.txt) <(grep -v '^#' reference/jobs2.txt)   # 应无差异

# 4) 快速冒烟测试（只算前 3 类，验证流水线/打印路径，总数会按设计 MISMATCH）
./build/fj_sudoku.exe --limit 3
```

命令行选项：`--threads N`（线程数）、`--emit-jobs`（只导出作业表）、`--ref DIR`（对拍数据目录，默认 `reference`）、`--limit N`（调试：只算前 N 类）、`--quiet`。

---

# 第二部分：组合计数（push the boundary）

第一部分的枚举法，工作量正比于终局总数（$\sim 7\times10^9$ 个叶子），这是它的天花板。第二部分换一个支点：**数"类"而不数"格局"**，把 $N_0$ 解构成一个与终局总数无关的、常数级的求和——全程不枚举任何一个完整终局，单线程 **~0.9 s** 得到精确的 22 位 $N_0$。

## 15. 核心因子化：三带列签名

把整盘看作三条横带 $B^1,B^2,B^3$（各 $3\times9$）。约束只有两类：① 每条带自身合法（3 行各是排列、3 宫各含 1–9）；② **列约束**——每一列的 9 格恰是 $\{1..9\}$。**带与带之间没有任何别的耦合。**

定义带 $B$ 的**列签名** $\sigma(B)=(T_0,\dots,T_8)$，$T_j$ = 第 $j$ 列那 3 个数字的集合（宫约束 ⟹ 每宫的 3 列三元组划分 $\{1..9\}$）。列约束就是：三条带在每列的三元组**划分** $\{1..9\}$。于是得到一个干净的因子化：

$$
\boxed{\,N \;=\; \sum_{\substack{(\sigma^1,\sigma^2,\sigma^3)\\ \text{逐列划分}}} B(\sigma^1)\,B(\sigma^2)\,B(\sigma^3)\,}
\qquad B(\sigma)=\#\{\text{列签名为 }\sigma\text{ 的合法带}\}
$$

**只要三个签名逐列划分 $\{1..9\}$，任取三条对应带拼起来必是合法终局。** 这把"数 $10^{21}$ 个终局"变成"对签名三元组求加权和"。再定义顶带 $\sigma^1$ 的**补全数**

$$
C(\sigma^1)=\sum_{(\sigma^2,\sigma^3)\,\text{划分}\,\sigma^1} B(\sigma^2)\,B(\sigma^3)
$$

它恰好等于**任何列签名为 $\sigma^1$ 的顶带补全成整盘的方式数**——这正是第一部分逐类暴力数出来的那个量。两条路在此汇合。

## 16. $B(\sigma)$ 与 $2^{\text{环}}$

$B(\sigma)$ 是"给定每列 3 个数字，有多少种把它们填进 3 行使每行成排列"。优雅的算法：

1. 先放 **row0**（一个横截：每列取一个数字，9 个互不相同 = 排列）；
2. 剩下每列 2 个数字，构成一个**二部 2-正则图**（列结点 ↔ 数字结点），它必是若干**不相交的环**；
3. 一个 2-正则图的真 2-染色（给出有序的 row1、row2）数目恰为 $2^{\#\text{环}}$。

$$
B(\sigma)=\sum_{\text{row0 为横截}} 2^{\,c(\text{残差图})}
$$

把内层"枚举 row1"换成一次 $O(9)$ 的环计数（并查集）。`src/combinatorial.cpp` 实现并验证：固定 box1 为规范型 $P$，$\sum_{\text{box2,box3}}B = 564\,350\,976 = 948\,109\,639\,680/1680$（合法带总数 / box1 的 1680 种列内容）✅。

## 17. 列兼容与 $C(\sigma)$

给定 $\sigma^1$，中带 $\sigma^2$ 的每个宫必须与 $\sigma^1$ 对应宫**逐列不相交**；底带 $\sigma^3$ 则被逐列取补**唯一确定**。实测每个宫的兼容划分数恰好

$$|\,\text{compat(box)}\,| = 56$$

（即文献中的 $\mathrm{CC2}=56$）。于是 $C(\sigma^1)$ 是一个 $56^3$ 项的和：

$$
C(\sigma^1)=\sum_{\substack{u_1\in\text{compat}(P)\\u_2\in\text{compat}(b_2)\\u_3\in\text{compat}(b_3)}} B(u_1,u_2,u_3)\cdot B(w_1,w_2,w_3),\quad w_k=\overline{\sigma^1_k\cup u_k}
$$

## 18. 桥梁与亚秒优化

**验证桥梁。** `equiv.c`/基线给出的 44 个解数 $\text{solcount}_i$，是"下 6 行序约简 ÷72"后的补全数；而 $C(\sigma^1)$ 是完整补全数。所以

$$C_i \;=\; 72\cdot\text{solcount}_i \quad(\text{全部 44 类逐一吻合})$$

聚合 $N_0 = 9!\cdot72\cdot\sum_{i=1}^{44}\text{mult}_i\,C_i = 6\,670\,903\,752\,021\,072\,936\,960$，精确。

**冲进亚秒。** 朴素实现 60 s，靠两步优化降到 ~0.9 s：

- **relabel 不变性**：$B$ 在重标 + 行/列/宫置换下不变。把 box1 重标成 $P$ 后，另两宫各只剩 **280 种"形状"**（列序无关的宫划分）。于是 $B$ 退化成一张**稠密 $280\times280$ 查表** `Btab` —— 热点循环里每次取 $B$ 都是一次数组访问，**消除全部哈希**。
- **`rel` 预表 + 锚点裁剪**：把"重标到 $P$"的规范化预计算成 `rel[anchor][box]`；而热点循环的锚点只来自 $\text{compat}(P)\cup\text{compl}(P)$（约 112 个），只建这些行。

性能曲线（同一个 $N_0$，越来越巧）：

| 阶段 | 方法 | 墙钟 | 关键 |
|--|--|--:|--|
| M0 | 枚举 ~70 亿终局（24 线程位运算 DFS） | 344 s | 第一部分 |
| M5 | 组合公式 + 朴素 memo | ~60 s | $B=\sum 2^{\text{环}}$ |
| M6 | relabel 规范化 memo | ~6.5 s | memo 1.3M→39k |
| M6 | 列签名→280 形状→稠密查表 + 锚点裁剪 | **~0.9 s** | 无哈希 |
| — | └─ 其中"纯组合求和"本身 | **~0.03 s** | 其余为一次性建表 |

相对基线约 **380×**；核心求和相对基线约 **一万倍**。

## 19. 三法对比与交叉验证

| 方法 | 枚举终局？ | 等价类 | 墙钟 | 验证闸门 |
|:--|:--|:--|--:|:--|
| **枚举（基线, `main.cpp`）** | 是（$\sim7\times10^9$ 叶子） | 71 | 344 s / 24 线程 | $N_0$；71/71 对 `results2.txt` |
| **模板法（`reference/equiv.c`, Ed Russell）** | 是（模板位运算加速） | 44 | 参考 oracle | $N_0$（文件头 44 行） |
| **纯组合（本作, `combinatorial2.cpp`）** | **否** | 44（+280 形状） | **~0.9 s / 单线程** | $N_0$；44/44 的 $C_i=72\,\text{solcount}_i$ |

```text
┌─ 交叉验证网（三条独立的路殊途同归到同一个 22 位数）──────────────┐
│  枚举基线 ── 71 类 ── Σ S_i c_i ─┐                                │
│  equiv.c  ── 44 类 ── 模板计数 ──┼─→  N0 = 6670903752021072936960 │
│  纯组合   ── B(σ)·B(σ)·B(σ) ─────┘     (全部精确逐位一致)          │
│  另：组合法的 44 个 C_i  ==  72 × 基线/equiv.c 的 44 个 solcount   │
└────────────────────────────────────────────────────────────────────┘
```

**这一章的洞见**：枚举法的代价绑定在终局总数上，无论怎么并行都逃不出"数 70 亿次"；组合法把这条绑带剪断——代价只与"签名类的数目"有关，于是同一个 $N_0$ 从 5 分钟坍缩到亚秒。而 `equiv.c` 的模板法恰好是两者之间的桥：它仍枚举，但用位模板把每类的枚举加速，并已经把类数压到 44。三种方法、三套独立代码、同一个 22 位数——这就是对"$6\,670\,903\,752\,021\,072\,936\,960$"最坚实的信心。

构建与运行第二部分：

```bash
g++ -O3 -mtune=native -mpopcnt -funroll-loops -std=c++20 -Isrc \
    src/combinatorial.cpp  -o build/combinatorial.exe    # M4: B(σ) 闸门
g++ -O3 -mtune=native -mpopcnt -funroll-loops -std=c++20 -Isrc \
    src/combinatorial2.cpp -o build/combinatorial2.exe   # M5/M6: 组合算 N0
./build/combinatorial2.exe        # ~0.9s, 打印 44/44 与 N0
```

---

## 20. 尾声：彻底自洽——44 类只是优化，而非必需

走到这里本可收尾，但还剩最后一点"生成痕迹"：§18 的聚合借用了 `mult_i`（对 36288 条带目录的归并计数）。既然 $B(\sigma)$ 已在手，外层权重也能纯组合地算出来。对 box1 用重标的传递性（$S_9$ 在 1680 种宫内容上传递，$B,C$ 重标不变）：

$$
N \;=\; \sum_{\text{所有签名三元组 }(\sigma^1,\sigma^2,\sigma^3)\text{ 列划分}} \!\!B(\sigma^1)B(\sigma^2)B(\sigma^3)
\;=\; 1680 \times\!\! \sum_{b_2,b_3\,\in\,1680^2}\!\! B(P,b_2,b_3)\,C(P,b_2,b_3)
$$

`src/combinatorial3.cpp` 据此计算，**全程只用 $B(\sigma)$**：不生成任何带目录、不读 `ed44.txt`、不用 `mult_i`、不做 44/71 类归并。$C(P,b_2,b_3)$ 在列置换/宫互换下不变，故只依赖 $(\text{shape}(b_2),\text{shape}(b_3))$，按形状对记忆化。结果：

$$
\sum_{b_2,b_3} B\cdot C \;=\; 3\,970\,776\,042\,869\,686\,272 \;=\; 2^{16}\cdot3^{7}\cdot 27\,704\,267\,971,\qquad
1680\times\text{它}\;=\;N_0\ \checkmark
$$

（单线程 ~28 s——这一版以速度换"纯粹"：$C$ 被独立计算了 39340 次而非借助 44 类。）

**这一步浮现的小定理：** 那 44 个等价类，本质上只是 $C(\sigma)$ 的一个**记忆化加速**，而非逻辑上的必需品——$N_0$ 可以**仅由带签名计数 $B(\sigma)$ 与列划分结构**推出。至此，"组合计数"做到了字面意义上的彻底：**全程不生成、不枚举任何一个格局或带，只做计数。** 这一笔，画得干净。

---

## 14. 参考文献

1. B. Felgenhauer, F. Jarvis, *Enumerating possible Sudoku grids*, June 20 2005.
   原始 PDF 见 `reference/sudoku.pdf`。
2. B. Felgenhauer, F. Jarvis, *Mathematics of Sudoku I*. 算法与数据页：
   `http://www.afjarvis.org.uk/sudoku/bertram.html`（原始程序 `sudoku_equiv.cc`、`sudoku2.cc`、数据 `jobs2.txt`、`results2.txt` 均在 `reference/`）。
3. Ed Russell，独立验证程序 `equiv.c`（"先放所有 1，再放所有 2…"，44 类 + 模板计数，与 FJ 结果一致；文件头即 44 类表）。
4. 拉丁方计数：S.F. Bammel & J. Rothstein (1975)；B.D. McKay & E. Rogoyski (1995)；B.D. McKay & I.M. Wanless（11 阶拉丁方）。
5. Kjell Fredrik Pettersen（"kjellfp"）/ G. Stertenbrink，band-generator 组合计数法（亚秒级），见 enjoysudoku 论坛 "Su-Doku's maths" 长帖与 archived Wikipedia *Talk:Mathematics of Sudoku*；本作第二部分由第一性原理独立推导，并以已知 $N_0$ 与 `equiv.c` 双重对账。
6. Russell & Jarvis, *Mathematics of Sudoku II*（对称群 / 5,472,730,538 个本质不同终局，`reference/sudoku_paper_extracted.txt` 同源站点 `sudgroup.html`）。

---

*本文档及配套 C++ 实现为 Felgenhauer–Jarvis (2005) 数独终局枚举的独立复现。所有中间产物（71 个等价类、71 个补全数、聚合总数）均与原始论文及程序逐字节/逐值对拍一致。*
