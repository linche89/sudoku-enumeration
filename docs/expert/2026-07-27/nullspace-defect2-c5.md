# nullspace-defect2-c5 (agent report, 2026-07-26 pre-flight sweep)

## Summary

Extracted and exactly certified the 2-dimensional null space of the C=5 completion operator U_3 = R_34*R_45 (16150 x 355, rank 353). One null vector is sparse with support 3: R(x,q0) - 3 R(x,q2) + 4 R(x,q39) = 0 for every 3-row state x, supported on patterns (5,5), (4,4,1,1), (3,3,2,2) — the exact lift of the C=4 relation (1,-4,9) on (4,4), (3,3,1,1), (2,2,2,2): both are the two-complementary-pair family {a^(C-k), abar^(C-k), b^k, bbar^k}, k=0,1,2, restricted to the classes whose d(a,b)-orbit contains 1. The second vector is dense (support 354/355) but has exact arithmetic structure z_q = kappa_q * ell_q / 6300 with kappa_q small ODD integers in [-17,15]; at C=4 the dense partner is exactly eps_q * ell_q / 70 with eps in {0,+-1} (verified U(eps*ell)=0 over Z). Pushed through R_45, the whole null space vanishes on 13200 of 17120 layer-4 states and the sparse vector's image is supported on just 6 layer-4 states (all "five complementary hole-pair" states) with coefficients (+-1,...,+5), the +5 on the fully aligned state; rank(U_4)=355 is full, so the defect is created entirely at the 3->4 transition. A sharp checkable C=6 conjecture is formulated.

## Details

VERIFICATION CHAIN (all exact; nothing under E:/Code/sudoku_FJ modified — read/copy only; all work in the scratchpad):
1. Copied E:/Code/sudoku_FJ/experiments/proto/layer_dp_gate.cpp to scratchpad/layer_dp_gate_copy.cpp; added a --dump-rmat PREFIX flag that dumps the exact integer sparse transitions R_{C-2}, R_{C-1} ("parent child K" triples) and per-layer state data (index, stab, T, masks) for layers C-2..C; recording is enabled whenever --rank or --dump-rmat is given; the dump executes only after the built-in N gate has passed. Compiled with g++ -O2 -march=native -std=c++17 -fopenmp -lpsapi (MinGW 13.2).
2. C=4 control run: N(4)=29136487207403520 OK; rank(U_3)[54x26]=24 mod both engine primes (defect 2). C=5 run (--threads 8 --caps 200,20000,20000,600 --rank --ref --dump --dump-rmat): N(5)=1903816047972624930994913280000 OK; all 355 reference triples matched; layers 1/107/16150/17120/355, zero holes; rank(U_4)=355 (FULL), rank(U_3)=353 mod 2^61-1 and mod 4611686018427387847. Dumps: c5_R3.txt nnz=10,859,910 (maxK=160), c5_R4.txt nnz=380,140 (maxK=14400). Log: scratchpad/c5_run.log.
3. Python pipeline (scratchpad/nullspace_c.py): exact int64 sparse product U_3 = R_3 @ R_4 (dense 16150x355, max entry 57,600; overflow excluded by comparing against two independent mod-p products); RREF mod 2147483629 and 2147483587 (rank 353, identical pivot sets); CRT + rational reconstruction of the nullspace; then EXACT integer verification, recomputed from the raw sparse files with pure-Python bignums: U_3 z = 0 over Z for both basis vectors against every one of the 16150 rows. Since rank_Q >= rank mod p = 353 and two exact independent integer null vectors exist, the defect over Q is exactly 2. The identical pipeline run on the C=4 dumps reproduces the documented relation (1,-4,9) verbatim, validating the method end to end.

FINDINGS:
A. Sparse relation (support 3, primitive integer form; qids in the native_c5 csv ordering):
   +1 * R(x, q0)  - 3 * R(x, q2)  + 4 * R(x, q39) = 0 for every 3-row state x, where
   q0  = (5,5)      ell=252,   m=16, F=26011238400  (words a x5, abar x5)
   q2  = (4,4,1,1)  ell=6300,  m=80, F=5202247680   (a x4, abar x4, b, bbar; |a xor b| orbit {1,4})
   q39 = (3,3,2,2)  ell=25200, m=80, F=2601123840   (a x3, abar x3, b x2, bbar x2; |a xor b| orbit {1,4})
   This is the natural lift of C=4's (1,-4,9) on (4,4), (3,3,1,1), (2,2,2,2): the two-complementary-pair family {a^(C-k), abar^(C-k), b^k, bbar^k}, k=0,1,2. Support selectivity confirmed at both C: the family patterns have additional realized classes — C=5 has a second (4,4,1,1) class (qid1, d-orbit {2,3}) and a second (3,3,2,2) class (qid13, d-orbit {2,3}), both with coefficient 0; at C=4 likewise only the d-orbit-{1,3} members carry weight, the {2}-orbit and non-complement-closed (2,2,2,2) members do not. On the family, F_k = F_0 / binom(C,k) exactly (C=4: 1,4,6; C=5: 1,5,10).
B. Dense partner: no second sparse direction exists (projective-ratio sweep over the 2-dim lattice: minimal support among directions independent of z_A is 354). Exact structure found: z_B,q = kappa_q * ell_q / 6300 where every kappa_q is a small ODD integer in [-17, 15] (off z_A's support; equivalently z_q * denom_q is 576 * odd, denom_q = prod of word-multiplicity factorials). In the same coordinates z_A is kappa = (25, -3, 1). At C=4 the dense partner is exactly (b2 + 4 b1) = eps_q * ell_q / 70 with eps in {0(x9), +1(x12), -1(x5)}, verified U*(eps.ell) = 0 over Z.
C. Honest negative results on the dense vector's coefficient map: kappa is NOT constant on mask-multiplicity patterns; NOT a linear functional of the pairwise Hamming-distance census m_d of the word multiset (exactly inconsistent); NOT explained by translation-invariant Fourier invariants sum s_u^2, sum s_u^4, or symmetrized triple products s_u s_v s_{u+v} (s_u = sum_i (-1)^{u.w_i}; exactly inconsistent); at C=4 eps is likewise not determined by word-size parity or the |s_u| spectrum multiset. Established structure is the divisibility/integrality (kappa odd, |kappa| <= 17) and the layer-4 localization below; the sign/magnitude map itself remains unexplained.
D. Layer-4 localization (the mechanism): rank(U_4) = 355 full, so the defect is created entirely at the 3->4 transition; the null space's forward image w = R_45 z is the 2-dim space null(R_34) intersect range(R_45). Both w-vectors vanish on 13,200 of 17,120 layer-4 states. w_A = R_45 z_A is supported on exactly SIX layer-4 states — all with 10 distinct masks, every box missing exactly twice, each state consisting of five complementary hole-pairs (a mask pair with hole at box b, complementary on the 4 used boxes, for each b) — with coefficients (after dividing by 1152): +1, -1, -1, +1, +1 on five "twisted" states and +5 (= C) on the fully aligned state (all pair-words equal; m=16, stab=240). The dense partner's image is +-1 * ell_y on ~90% of its 3,915-state support with kappa_w in {+-1, +-2, +-3}.

C=6 CONJECTURE (sharpest checkable form):
 C1 (defect and location): rank(U_3^(6)) = N_6 - 2 mod any prime (N_6 = number of complete C=6 classes), with rank(U_4^(6)) and rank(U_5^(6)) full — the defect 2 is created at the 3->4 transition. Checkable with the existing --rank machinery (M_3 = 12,324,872 parents).
 C2 (sparse relation, main claim): one null direction is supported exactly on the realized complement-closed two-pair family classes {a^(6-k), abar^(6-k), b^k, bbar^k}, k = 0..3 — patterns (6,6), (5,5,1,1), (4,4,2,2), (3,3,3,3) — restricted by the d(a,b)-orbit rule, with signs alternating as (-1)^k. Two variants that agree on all C<=5 data but diverge at C=6: (i) supported iff the orbit {d, 6-d} contains 1 (then the {3}-orbit classes are excluded); (ii) supported iff min(d, 6-d) is odd (then {3}-orbit classes are included). Equivalent sharp statement: the column restriction of U_3^(6) to the family classes has corank exactly 1, and the full null vector is its zero-extension. This is testable WITHOUT building all of U_3: take a few hundred 3-row states x, compute only the family columns R(x, q) by targeted enumeration (these classes are tiny and maximally symmetric), and check corank 1 and which d-orbits carry weight.
 C3 (coefficient candidate, LOW confidence — 2 data points fitting 2 parameters): writing z_k = (-1)^k f_C(k), the data give f_4(k) = (k+1)^2 and f_5(k) = (2 + 5k - k^2)/2; the unique quadratic-in-k / linear-in-C interpolation f_C(k) = 1 + (C/2) k + (7 - 3C/2) k^2 predicts C=6 coefficients (1, -2, -1, +8) for k = 0..3. Treat strictly as a candidate to test; C2's corank-1 claim pins the relation up to scale independently.
 C4 (dense partner): the second null vector has the form z_q = kappa_q * ell_q / D with all kappa_q small integers (C=4: {0,+-1}; C=5: odd, |kappa| <= 17) — an integrality certificate checkable coordinate-wise from a mod-p nullspace of a row-sampled U_3^(6).
 C5 (localization): the image of the null space under the forward transitions vanishes on the vast majority of intermediate-layer states, and the sparse relation's image at the layer whose masks miss one box is supported on O(C) states of the complementary hole-pair type, with weight +-C on the fully aligned state.

KEY FILES (scratchpad = C:/Users/CHOPPE~1/AppData/Local/Temp/claude/E--Code-sudoku-FJ/c1528767-e58c-4a4d-8325-aaf8b87b1d06/scratchpad):
 layer_dp_gate_copy.cpp/.exe (modified engine copy), c5_run.log (all gates), c5_R3.txt / c5_R4.txt / c5_layer{3,4,5}.txt / c5_classes.csv (exact dumps), nullspace_c.py + c5_null.log (certified extraction; C=4 control identical pipeline), c5_nullbasis.txt (exact integer basis: line 1 = sparse z_A, line 2 = dense z_B), c5_U3.npy (exact dense U_3, int64), c5_kappa_table.csv (per-class qid, pattern, ell, m, F, z_A, z_B, kappa), c5_wvecs.txt (layer-4 images w1, w2 with ell_y), analyze2..8.py (structure probes), c4_* (C=4 control artifacts). Reference CSV used: E:/Code/sudoku_FJ/docs/expert/2026-07-21/native_c5_response_quotient_triples.csv (355/355 matched before dumps were trusted).

## Verdict

SUCCESS: C=5 null space certified exactly over Z (defect exactly 2); sparse relation (1,-3,4) on the (5,5)/(4,4,1,1)/(3,3,2,2) family is the clean lift of C=4's (1,-4,9) with the same d(a,b)-orbit selectivity; dense partner has exact odd-kappa*ell integrality structure but its coefficient map resisted all low-order invariant explanations (reported honestly); decisive, cheaply checkable C=6 conjectures formulated (corank-1 family restriction, d-orbit rule variants that diverge at C=6, candidate coefficients (1,-2,-1,8), integrality certificate).

