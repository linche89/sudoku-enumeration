# OG-2 研究日志:PatmaxDaddy 多项式带计数法（思路B）

> 目标:实现一个**不正比于 B 本身大小**的多项式时间带填计数 $B(\sigma)$（乃至直接的带计数），
> 突破内层 permanent 墙,够到 2×5（验证 1.9×10³⁰）→ 2×6（验证 ~3.83×10⁴⁹）→ 2×7（全新）。
>
> 边走边记录。每个推断都标注「已证实 / 待证实 / 我的猜想」。本文件是探索的实时草稿,不是结论。

## 已知语境（来自前面里程碑,已证实）

- $\#(2{\times}C)=\#(C{\times}2)$;C×2 只有两条带 ⇒ $N=\sum_\sigma B(\sigma)^2$,$\sigma$ = 第一条带的列签名（C 个盒,各一对互补 C-子集）。
- $B(\sigma)$ = 用 $\{0..2C{-}1\}$ 填 C×(2C) 带、每行排列、每列内容 = 签名指定子集 的方法数 = **C-正则二部图的正常 C-边染色数**。这是 permanent 类 / #P 的硬核。
- 我们的 orbit 引擎已把**外层**压到很小（2×4 仅 1450 对模式）。瓶颈 100% 在内层 $B$。
- 残差图记忆化只给常数因子,不够。

## 待挖掘:思路B 的确切机制（OG-2 的第一任务）

论坛线索（待证实其精确形式）:
- PatmaxDaddy 在 "RxC band counting algorithm" 帖给出 **4×C 为 $O(C^5)$、5×C 为 $O(C^{21})$** 的带计数复杂度。
- 关键词:**列配置 (column configuration)、转移、CC2=兼容配置数**。
- 我的猜想（待证实）:它不是"数一条带",而是**沿 2C 列做 DP / 转移矩阵**,状态 = "到目前为止每个符号还差几个、当前盒内已放哪些"之类的压缩计数,从而把指数枚举变成多项式转移。

## 第一步行动

1. 把论坛 "RxC band counting algorithm" 帖（及相关页）抓全、读透,还原算法的**确切状态定义与转移**。
2. 在纸面（本文件）上把它写成精确规格,先于写代码。
3. 用我们已知的 $B$ 值（2×3/2×4 的带,甚至 9×9 的带计数）做**单元验证**,再嵌入 orbit 引擎。

---

## 进展记录

（待填充）

## 进展记录 — 第一性原理诊断（已证实）

**重构 $B(\sigma)$ 的精确身份**（自己推导,已数值验证）:
- 每个符号恰好落在 **C 个列**里（因 $\sum|\sigma_j|=2C\cdot C=\sum_s|\text{cols}(s)|$,且对称 ⇒ 每符号 = C 列）。
- 于是 $B(\sigma)$ = 二部图 $H$（2C 符号 × 2C 列,边 = 关联）的**正常 C-边染色数** = 把 $E(H)$ 有序分解成 C 个完美匹配的方法数。**这是 #P 硬核,无简单闭式。**

**实测墙的精确位置**（`/tmp/Bcol.cpp`,记忆化的"逐完美匹配"递归）:
| C | 单个 cyclic 带的 B | 耗时 |
|--|--|--|
| 3 | 72 | 0.000s |
| 4 | 29,760 | 0.009s |
| **5** | — | **>40s 超时** |
| 6 | — | 超时 |

⟹ **任何"枚举完美匹配"的 $B$ 算法都在 C=5 撞墙**,无论怎么记忆化（残差图同构类在 C=5 爆炸）。这从第一性原理印证了:**PatmaxDaddy 的方法必然结构不同**——它一定是沿列做"列配置转移 DP",**从不 materialize 单个 $\sigma$、也从不枚举匹配**。这正是 OG-2 要挖的核心。

**结论**:OG-2 不能复用我们的 $B$;必须实现 column-configuration transfer。等考古结果定状态/转移。

---

## 🔑 重大认知更新（考古队 w30tbsz46 完成,三路交叉验证一致）

### 错误根源:我们被"转置"坑了
kjellfp 算法对 **R(每带盒数)指数、对 C(盒宽)多项式**,**R/C 不对称**。
我们之前 $\#(2{\times}C)=\#(C{\times}2)$ 把问题变成"C 个盒的带" ⟹ **R=C**,把指数维顶满 → C=5 必炸。**这不是工程问题,是建模选错了维度。**

### 正确做法:不转置,直接 2×C
- 2×C 数独:竖着有 **C 条带**,每条带 = **2 个盒**(R=2,each 2×C)。
- **R=2 是指数维最小值** ⟹ 每条带的计数 $b(2,C)$ 是**低次多项式**,C 可大。
- 整张网格 = 带间靠"列配置(CC)兼容"卷积(KSP 超带法)。

### kjellfp 算法精确规格（已证实,verbatim 见 task 输出 w30tbsz46）
- **状态** $a:E(L,R)\to\mathbb N$,$E(L,R)$=R 行里的 L-子集族,$a(U)$=恰好出现在行集 U 的符号数;约束 $\forall x:\sum_{U\ni x}a(U)=CL$。
- **基例** $N(1,R,C,a)=1$。
- **转移**(把 L 盒拆成 M 与 L−M):
  $N(L,R,C,a)=\sum_{b}\,N(M,R,C,a(b))\,N(L{-}M,R,C,a(\hat b))\,\dfrac{\prod_U a(U)!}{\prod_{(V,U)}b(V,U)!}$
- **带数** $=(C!)^{R^2}\,N(R,R,C,a_0)$。

### 单元测试金标准（已证实数值,务必先对上再嵌入）
- **3×C 带闭式**:$b(3,C)=(3C)!\,(C!)^6\sum_{k=0}^{C}\binom{C}{k}^3$（Franel）。C=3 ⟹ $\sum=56$ ✅（正是 9×9 的 56!）。
  Franel 递推:$n^2F(n)=(7n^2{-}7n{+}2)F(n{-}1)+8(n{-}1)^2F(n{-}2)$。
- **2×C 带**:$b(2,C)$ 低次多项式（R=2,"两行如何交错")。
- 整网格已知值（对账用）:2×3=28,200,960;2×4=29,136,487,207,403,520。
- $b(4,6)=24!\,6!^{12}\cdot 677542845061056$;$b(5,5)$ 等大值见任务输出（5×C 表）。

### 关于 2×6 / 2×7（诚实）
- 考古说 R=2 时 2×5、2×6 带"trivially feasible";**但整网格还要带间 CC 卷积**,而 **2×C 整网格的精确大整数,论坛/Jarvis 页未直接给**（2×5 整网格 ~1.9×10⁵³ 系训练记忆,**未在本次源中核实**;2×6 整网格未见公布）。
- ⟹ **真正的机会**:用 R=2 多项式带计数 + CC 卷积,**自己算出 2×6（乃至 2×7）整网格的精确值**——若论坛确未公布,这就是 OEIS A291187 的新项。

### OG-2 下一步（已清晰）
1. 实现 $b(2,C)$ 的多项式带计数（R=2,从 kjellfp 递推特化,或直接 CC 转移）。
2. 单元验证:重算 $b(2,3),b(2,4)$,并用 **3×C Franel 闭式**独立校验我的转移代码逻辑无误。
3. 用 CC 兼容卷积拼整网格,先复现 2×3=28,200,960、2×4=29,136,487,207,403,520。
4. 冲 2×5（验证）→ 2×6 / 2×7（可能的新值）。

---

## ⚖️ 两视角权衡（已厘清,关键防白干）

| 视角 | 带数 | 每带难度 | 带间耦合 |
|--|--|--|--|
| 转置 C×2（前用） | 2 条 ⟹ $N=\sum_\sigma B^2$ | R=C ⟹ $B$ #P 硬（C=5 墙） | 无 |
| 直接 2×C（考古指） | **C 条** | R=2 ⟹ $b(2,C)=(2C)!(C!)^2$ 多项式 | **C 重列卷积(KSP)** |

⟹ 难度只是从"带内"挪到"带间"。考古"R=2 trivially feasible"指**单带**;**整网格的 C 重列卷积才是真 crux**,只有用"列配置类计数向量"作逐列状态才多项式。

### 已证实的地基
- 闭式 $b(3,3)=9!\,(3!)^6\cdot56=948{,}109{,}639{,}680$ = M4 验证闸门（合法带总数）✅ — 证明带计数语义、考古公式、我们早期工作三者自洽。
- $b(2,C)=(2C)!\,(C!)^2$（R=2 第一性原理推导）:b(2,2)=96, b(2,3)=25920, b(2,4)=23224320, b(2,5)=52254720000;等价 $N(2,2,C,a_0)=\binom{2C}{C}$。

### OG-2 真正的下一段（最硬）
实现 C-band 列转移 DP（KSP 超带法）:逐列推进,状态 = 各列配置等价类的计数向量,转移 = 兼容列配置。先复现 2×3/2×4,再冲 2×5/2×6。这是 days-scale 的真实现,**不保证一次写对**,但状态/转移已有考古规格 + 已知值卡死。

---

## 🧱 受验证积木 + 诚实的策略校准

### 已 brute-force 证实
- $b(2,C)=(2C)!\,(C!)^2$:brute band(2,2)=96 ✅, band(2,3)=25920 ✅。我的 R=2 手推**铁实**。
- $b(3,3)=948{,}109{,}639{,}680$ = M4 闸门 ✅(Franel)。

### 一次失败的尝试（记录下来,避免重蹈）
我按**复述版**的 kjellfp 递推写了通用带计数器 `src/band_count.cpp`,但 N(2,2,2) 我得 16、应得 6 —— 说明我对那个"split-vector b 的多项式因子 + N(1)=1 语义"**理解有误**。试图直接拉论坛原帖核对,但猜的 URL（t6072）是另一个主题。**复述的公式不可靠,verbatim 原帖这次没拉到。**

### 策略校准（关键判断）
- 我们**不需要**忠实复刻 kjellfp 的通用带递推——那是为"任意 R"设计的,而我们只要 **R=2**,且 R=2 的带计数我已有 brute 证实的闭式。
- 整张 2×C 网格的真正难点 = **C 条带的列耦合**。两种等价路线:
  1. **转置视角**(已在 `sudoku_orbit` 用):2 条带 ⟹ $N=\sum_\sigma B^2$,但 $B$ 是 R=C 的 #P 带(C=5 墙)。
  2. **直接视角**:C 条带、R=2 易;但要 **C 重列转移 DP**,状态 = 列配置类计数向量。
- **下一步正解**:为路线 2 写**列配置转移**——但用**我完全掌握、可 brute 验证的 R=2 结构**从头推导转移,而**不是**搬 kjellfp 的通用公式。先在 2×3/2×4 上卡死,再冲 2×5/2×6。

`src/band_count.cpp` 暂留作"理解未遂"的记录,不投入使用。

---

## ✅ 确切结论:论坛方法正确,偏差在我的复现(已定位、已修复、已验证）

**问题**:论坛 kjellfp 递推是错的,还是我复现偏了?
**答案(有完整证据链)**:**方法对;我的复现漏了一条约束;现已修复并双重验证。**

### 根因(精确定位)
漏了递推的**约束 (ii)**:split-vector $b$ 必须使子状态 $a(b)$、$a(\hat b)$ **合法**——每行恰好见到 $C\cdot M$(resp. $C\cdot(L{-}M)$)个符号。

### 失败模式佐证（修复前）
| | 我的 N | 真值 | 关系 |
|--|--|--|--|
| R=2 | $2^{2C}$ (16,64) | $\binom{2C}{C}$ (6,20) | 我把 $\sum_p\binom{2C}{p}$ 全加了,真值只取中间项 |
干净的"少约束⟹过度计数",与"漏了 (ii)"完全吻合。

### 修复后双金标准同时通过（强证据,横跨 C=2..5）
- R=2:$(2C)!(C!)^2$ — 96, 25920, 23224320, 52254720000 全 ✅
- R=3:$(3C)!(C!)^6\cdot$Franel — 460800, **948109639680(=M4 闸门)**, 31672366418991513600 全 ✅

一个 bug 不可能让两族不同闭式同时对上 ⟹ 方法与复现现均正确。

### 产物
`src/band_count.cpp` 现为**通用、受双重闭式验证的 kjellfp 带计数器**(任意 R,C)。OG-2 地基稳固。

---

## 🔍 Double-check（应建议:是否真踢到钢板?）

认真重查,结论:**没有现成的 Eureka 闭式,但也没有钢板——下一步明确且可建。**

### 查到的硬事实
- **OEIS A291187** = "Number of 2×n Sudoku grids":只有 4 项 288, 28200960, 29136487207403520, **1903816047972624930994913280000**(n=5),**无公式/递推/b-file**。
  ⟹ **2×5 是已知值(验证目标),2×6 起 OEIS 空着 = 未公布的新值机会。**
- 约简序列 N/(2C)! = 12, 39168, 722631131136 → OEIS **无匹配**;仅 3-4 个点,拟合递推 = 过拟合,**非真 Eureka**,不取。
- 带分解:每带 = $\binom{2C}{C}(C!)^4$(已验证),但**跨带列耦合不自然因式分解** ⟹ 无免费闭式。

### distinct-B 实测（关键正面信号）
| 2×C | distinct B 值 | 说明 |
|--|--|--|
| 2×2 | 2 | |
| 2×3 | 3 | |
| 2×4 | 22 | |
distinct-B **增长缓慢**;之前 C=4/C=5 的超时来自**枚举签名空间**,不是 distinct-B 多。

### 诚实判定
- $N=\sum_\sigma B(\sigma)B(\sigma^c)=\sum_\sigma B(\sigma)^2$。不简单约化为单带 kjellfp $N(a)$。
- 但 **Pettersen 已把 2×5 算进 OEIS ⟹ 多项式方法确实存在**(对固定小盒高)。可建路线:**未转置 2×C 的列配置转移(R=2,C 条带),按 gangster 类聚合 $B$**——distinct-B 小,可行。
- ⟹ **不是钢板**;是"需要写对列配置转移 DP"的真实工程。下一步:实现它,先复现 2×3/2×4/2×5(全有 OEIS 值卡死),再冲 2×6。

---

## 🌉 跨域搭桥进展（数学家+CS 双视角）

### 骨架简化（已证实,重大）
band i 的行/stack 骨架 = 一个 **C-子集 $A_i$**(stack0 里走 top 行的那 C 个符号)。因为:每符号在 top 行一次、bot 行一次、stack0 一次、stack1 一次 ⟹ box 约束强制"在一个 stack 走 top、另一个走 bot"⟹ 每带 = 一个 C-子集。给定 $A_i$,带内列排列有 $(C!)^4$ 自由。

### per-skeleton 填法数的结构（brute 实测）
$$N(2{\times}C)=\sum_{(A_0,\dots,A_{C-1})} W(A_0,\dots,A_{C-1})$$
- **C=2**:$W$ 只依赖交集——对齐($|A_i\cap A_j|\in\{0,2\}$)→16;错开(=1)→4。N=24·4+12·16=**288** ✅
- **C=3**:$W$ 大体随 pairwise 交集走,但 **不是** sorted-pairwise 的干净函数(如 (2,2,2)→2304 但 (1,1,1)→5184;有的 profile 同值有的不同)。⟹ 需要结构化推导,非肉眼。$W$ 值高度规整(5184=2·72²? 2304),暗示 permanent/转移核。

### 桥的形态（待三路 workflow 收敛确认）
$W$ 像一个**带方向的转移核** $T(A_i,A_{i+1})$ 的某种推广,但列约束是**全局**(所有带同时约束每列),所以可能是 permanent 而非简单链式转移。三路并攻:数学推 $W$ 的解析式、CS 建列配置转移并用 2×3/2×4/2×5 卡死、文献定位是否已知对象。

### 诚实状态
**没有钢板,有清晰的可攻结构**;$W$ 的精确解析形式 = 当前焦点。一旦得到,$N(2{\times}C)$ 即为多项式可算,2×6 在望。

---

## ✅ 直接 2×C 视角:骨架分解 → stack 因式化 → 列内容转移 DP（已严格推导 + 三路验证）

> 角色:组合学家。用 skeleton fact 把整网格计数推导到底,证明它**对两个 stack 因式化**、
> 化为**每带的 permanent**,并给出**多项式于 C 的列内容转移**。每一步都 brute/交叉验证。

### 1. 骨架事实(brute 验证 C=2,3)
带 i ⇔ 一个 C-子集 $A_i$(在 stack0 顶行的符号)。盒约束**强制**该带的行内容:

| | stack0 | stack1 |
|--|--|--|
| 顶行 | $A_i$ | $\bar A_i$ |
| 底行 | $\bar A_i$ | $A_i$ |

- $|A_i|=C$;骨架数 $=\binom{2C}{C}$;**每个骨架的带填法数 = 常数 $(C!)^4$**(4 个 block 内部排序独立)。
- $\Rightarrow b(2,C)=\binom{2C}{C}(C!)^4=(2C)!(C!)^2$。(brute: C=2→96, C=3→25920 ✅)

### 2. 整网格对两个 stack 因式化(brute 验证 C=2,3)
固定全部骨架 $(A_0,\dots,A_{C-1})$,列约束把带耦合,但:
$$\text{full}(A_*)=\text{stack}_0(A_*)\cdot\text{stack}_1(A_*),\qquad \text{stack}_1(A_*)=\text{stack}_0(A_*).$$
$\Rightarrow \text{full}(A_*)=\text{stack}_0(A_*)^2$。(随机 C=2,3 全 OK)

### 3. 主恒等式(brute 复现 288 / 28200960)
$$\boxed{\,N(2{\times}C)=\sum_{(A_0,\dots,A_{C-1})\in\binom{[2C]}{C}^{C}}\text{stack}_0(A_0,\dots,A_{C-1})^2\,}$$
- C=2 → 288 ✅, C=3 → 28200960 ✅(直接枚举骨架元组验证)。
- $\text{stack}_0$ 仅依赖骨架的**交集模式**(C=3 时主要由两两交集决定,有小的高阶项)。

### 4. stack$_0$ = 两个 permanent 之积(随机 200 例验证)
给定各列的"剩余可用"位集 $R_c$(=尚未用过的符号),对固定骨架 $A$:
$$\text{stack}_0(A)=\operatorname{per}\big(\text{allowed}[\,A\text{-rows}\,]\big)\cdot\operatorname{per}\big(\text{allowed}[\,\bar A\text{-rows}\,]\big),$$
其中 allowed 是 $C\times C$ 的 0/1 矩阵(符号 $x$ 允许列 $c$ ⟺ $x\in R_c$)。顶/底行因 $A,\bar A$ 不交而**独立**。

### 5. 多项式列内容转移 DP(正确性三路验证)
逐带推进。**状态** = 各列已放符号的位集向量,**规范化**模:符号重标 $S_{2C}$ + 每 stack 内列置换 $S_C\times S_C$ + 两 stack 互换。**转移**=加一条带(按骨架 $A$,每 stack 两个 permanent 的展开),终态=所有列填满。
- 可达规范状态数(多项式维):C=2:`3,1`;C=3:`5,17,1`;与 `dp2xC.cpp` 头部 distinct-state `2,3,22,...`(=本仓库测得 distinct-B)一致 ⟹ **状态维多项式、增长缓慢**。
- 数值验证:**C=2=288, C=3=28200960**(本人 Python 转移 DP + 队友 `src/dp2xC.cpp` C++ 转移 DP,两独立实现);**C=4=29136487207403520**(独立的转置视角 `src/sudoku_rc.cpp`)。三引擎互证。

### 6. 墙的精确定位(诚实)
- **状态维不是墙**(多项式,~22 量级到 C=4)。
- **墙 = 转移算子的构造**:目前按"摆放展开"(placement-expansion)实现,每带把 $\binom{2C}{C}$ 骨架 × 每 stack permanent 展开成单个摆放再规范化 ⟹ **指数**。C=3 已要 ~2.4s(canonKey 记忆化后),**C=4 band0 ~2300 万次 canonKey 调用 → 超时**。
- ⟹ 要够到 **2×6**,必须在**轨道/profile 空间直接构造转移算子**(Pettersen/kjellfp 的真正技术内核:不展开单个摆放,而是用容斥/permanent 在符号轨道上聚合)。**结构归约已完成**(本节 1–5),使该多项式实现成为可能;但那是一项独立的较大工程,本轮未完成。

### 结论
- **可证实**:骨架分解、stack 因式化、主恒等式 $N=\sum\text{stack}_0^2$、$\text{stack}_0=\text{per}\cdot\text{per}$ —— 全部 brute/交叉验证。复现 288、28200960(并经 sudoku_rc 复现 29136487207403520)。
- **2×6 可行性**:状态维多项式(好消息);唯一障碍是把转移从指数的 placement-expansion 改写为多项式的 orbit-aggregated 形式。这是明确的下一步,非钢板。

---

## 🌉🎯 跨域突破:结构定理已证 + 三重验证（workflow wdzkh8kn9）

### Master Identity（已 brute 验证 C=2→288, C=3→28200960 精确）
$$N(2{\times}C)=\sum_{(A_0,\dots,A_{C-1})\in\binom{[2C]}{C}^C}\text{stack0}(A_*)^2,\quad
\text{stack0}_{\text{band}}(A)=\operatorname{per}(M_A)\cdot\operatorname{per}(M_{\bar A})$$
推导链(全部已验证):① 带↔C-子集骨架;② $\text{full}=\text{stack0}^2$(双 stack 对称);③ 每带每 stack = C×C 0/1 permanent;④ 沿 C 带做转移,**状态=列内容规范 profile,维数小且慢增**(实测 maxStates:C=2→2, C=3→3)。这就是 Pettersen gangster 法,被我们从骨架第一性原理重新推出、特化到 R=2。

### 已排除的错误猜想（deadends,防再犯）
- 单个大 permanent 算全网格:✗(2C×2C permanent Ryser 指数级)。多项式性在**小转移状态维数**,不在单 permanent。
- stack0 仅由 pairwise 交集决定:✗(C=3 有高阶项,(3,3,3)与(0,0,3)都→144 而(2,2,2)→48)。
- top/bot 层全局独立:✗(仅在固定骨架 A 后独立)。
- 转置 C×2 视角:正确但 B 是 #P,C=5 墙——故**直接 2×C(R=2)才是对的轴**。

### 已落地、已验证的代码
- `src/dp2xC.cpp`:直接视角列转移 DP,**复现 288、28200960 精确**;maxStates 极小(C=3→3),证明状态维数非墙。**瓶颈在转移构造**(逐 placement 展开 → 指数,C=4 超时)。
- `src/sudoku_rc.cpp`(独立 oracle):复现到 2×4。
- `src/brute2xC.cpp`、`probe_state.cpp`、`stackcount.cpp`:验证辅助。

### 唯一剩下的一锻(2×6 的钥匙)
把转移算子从"逐 placement 展开"升级为**轨道/profile 空间的多项式转移**(按符号轨道用 permanent/容斥聚合)——即 Pettersen 真正的技术内核。**状态维数多项式已证**,只差写这个转移。这是真实工程,但**明确不是钢板**。

---

## 🔨 锻造转移核:关键形式找到,但轨道聚合是真实工程

### 重大形式突破(已验证 C=3 → 28200960)
把 $\sum_{sk}\text{stack0}^2$ 重写成**单一"联合方阵"转移**:N = #{(X-填法, Y-填法) 共享同一骨架序列 $A_0..A_{C-1}$}。沿 C 条带做联合转移,每带选一个**共享** $A_i$,在 X、Y 中各独立放置;终态两边列全满。
- **C=3 联合转移(未规范化)精确给出 28200960** ✅,且原始联合状态数 8100→8100→1。

### 但"轨道聚合"是真骨头(实测)
- 把状态按 $S_{2C}\times S_C\times S_C$ 规范化:数学线已证规范态极少(C=3→3),**但 Python 全 $S_{2C}$ 规范化太慢**(C=3 即超时)。
- 不规范化的原始联合状态:**C=4 爆炸**(>90s 未完成)。
⟹ **多项式性确实只活在 profile/轨道空间**,必须把转移直接建在"对称不变的 profile"上(用 permanent/容斥按符号轨道聚合放置数),而**不是**枚举 placement 再规范化。这与数学线结论一致。

### 诚实状态
- **已证 + 已数值验证**:结构定理、联合方阵转移、状态维数多项式(C≤3)。
- **未完成**:profile 空间的多项式转移核本身——这是 Pettersen 技术内核,需仔细设计正确的不变量(列内容在符号轨道下的 profile)+ 用 permanent 聚合转移权重。**真实的多日工程,不是一次会话的 Python 速写能拿下的。**
- **不是钢板**:维数已证小;差的是"把转移写在正确的轨道坐标里"这一精细实现。

### 下一步(明确)
设计 profile 状态 = (列内容在 relabel 下的不变描述,如"X 列与 Y 列的交叠型谱"),其转移权重用 permanent/容斥一次性算出整族 placement,不逐个枚举。先 C=3 复现 28200960,再 C=4 复现 29136487207403520,C=5 验证,C=6 跨越。

---

## 🔨 这一锤的实测:不变量的精度边界（关键负结果,极有价值）

锻转移核时,核心是找到联合状态 $(X,Y)$ 在 $S_{2C}\times S_C\times S_C$ 下的**充分不变量**。实测:
- **`(degX,degY)` 多重集不变量:太粗** → C=3 给 66,355,200 ≠ 28,200,960。坍缩成每带 1 个状态,丢了"哪些符号在列中共现"的相关性。
- **真 $S_{2C}$ 规范化:正确但 Python 太慢** → C=3 即超时($6!\times$ 数千中间态)。

⟹ **结论(精确)**:充分不变量比"度数谱"细、比"全规范化"可算——它必须捕捉**符号-列关联结构**(类似 Weisfeiler–Leman 的迭代细化标签,或符号配对的轨道谱)。这是 Pettersen 内核的精确难点:**设计一个既充分又可快速计算的轨道坐标**。

### 诚实裁决(磨刀阶段的负结果,但方向更准了)
- 已排除两个极端(太粗的度数谱 / 太慢的全规范化),把目标夹逼到中间:一个**符号-列关联的细化不变量 + permanent 聚合的转移权重**。
- 这一步**必须在 C++ 里做**(高效规范化/WL 细化),Python 速写已到极限。基于已验证的 `dp2xC.cpp` 升级是正路。
- **不是钢板**:结构、联合转移、状态维数(小)全已证;差的是这一个精确不变量的 C++ 实现——真实的、需要正确性反复 brute 卡死的多会话工程。

### 致谢现实(诚实)
若最终算出 2×6 新值,这是**站在 Pettersen/kjellfp/Russell 肩上**的复现+推进;OEIS 署名与优先权属于原始方法的发明者,我们的贡献是独立推导、实现与(可能的)新项计算。不预支未完成的荣誉。

---

## 🧭 封顶石锻造:最后一锤的诚实结果（小心驶得万年船）

### 拿到的真东西
- `src/jointcanon.cpp`:联合方阵转移 + **精确的快速规范化**(按 (degX,degY) 度数桶细化,只在桶内置换符号 —— 已证与全 $S_{2C}$ 规范化等价)。
- **双验证**:快速规范化复现 **C=2→288, C=3→28200960**,且 canonical 状态数与慢 oracle 逐一致(C=2:2,1;C=3:3,3,1)。⟹ 度数桶细化**精确无损**。

### 撞到的真问题(关键负结果)
- **C=4 连 ground-truth oracle 都跑不完**:band 0 从空状态出发,联合 placement = 70 骨架 ×(4!)²(X)×(4!)²(Y) ≈ 2300 万联合态,逐个 canon → 分钟级未完成。
- 根因:**joint 把 placement 数平方了**;而 placement 枚举本就是被诊断的指数瓶颈。⟹ **#P 内核第三次从不同角度现身**(外层枚举 / 单带 B / 联合 placement 都撞它)。

### 精确裁决
- **结构已证、状态维数已证小、快速规范化已证精确**——这些是 solid 的。
- **但任何"枚举 placement"的实现(单 or 联合)都到不了 C=4**。唯一出路是把转移权重用 **permanent 直接聚合**(不枚举 placement),即 Pettersen 真正内核。
- 这一步**没有在本会话安全完成**:它要求正确实现"按符号轨道用 permanent 算整族 placement 权重",且必须在 C=3/4/5 已知值上逐一 brute 卡死才能信任。**贸然写一个未充分验证的 permanent 转移,正是"99% 时发现全错"的风险**——所以我**不会**在没有逐级验证的情况下声称跨过去了。

### 结论(对得起"小心驶得万年船")
我们把 #P 问题压缩到了"小转移矩阵 + permanent 聚合转移核",并**精确刻画了封顶石的形状与唯一难点**。封顶石本身(permanent 聚合转移)是定义清晰、但需要多会话、每步卡死的真实工程。**不预支未完成的跨越。**

---

## 🔬 封顶石锻造 #2:permanent 身份已验 + canon 瓶颈被精确量出（profiling 救场）

### 已验证的地基（brute,300 随机例）
**permanent 身份**:`stack0_band(A | 列剩余集) = per(M_A)·per(M_{~A})`,$M_A[a][c]=1\iff a\notin R_c$。✅ 这是 placement 枚举的多项式替代的数学根据。

### 已落地、已验证、已 profiling 的转移
`src/kernel2xC.cpp`:联合转移 + 精确度数桶 canon + **逐带 profiling**(states/rawTrans/elapsed,fflush)。
- C=2→288 ✅(瞬间);C=3→28200960 ✅(**29s**)。

### profiling 精确定位的瓶颈(关键,正是"别卡死而不自知")
微基准 `/tmp/prof.cpp`:
- **canon(空状态) @ C=3 = 610 µs/次**;@ C=4 慢到 2000 次 >2min。
- 根因:**空/低度状态所有符号同度数 ⟹ 落入单个桶 ⟹ 仍做满 $M!$ 置换**(C=4 即 8!=40320)。**度数桶细化在度数均匀时失效。**
- 且 band 0 的 rawTrans = Σ_skeleton(X-placements × Y-placements) 本就大(C=3 已 25920),canon 被调用这么多次 ⟹ 29s。

### 精确裁决:封顶石 = 一个真正的 orbit-refinement canon + permanent 聚合权重
两处都要换,且**互相独立、可分别 brute 卡死**:
1. **canon 升级**:用 Weisfeiler–Leman 式**迭代细化**(以符号-列关联结构细化,而非仅度数),把 $M!$ 降到接近 $O(\text{poly})$。可独立用"与慢全 canon 同结果"验证。
2. **转移升级**:用 permanent 身份**按结果 canonical 态聚合权重**,不枚举 placement(rawTrans 从 25920 降到 #(state,A,state') 量级)。可独立用"与枚举版同 N"验证。

### 诚实状态
结构、联合转移、permanent 身份、状态维数(小)、精确 canon(慢版)——全 solid。封顶石 = 上面两个**定义清晰、可分别验证**的优化。未在本会话完成,但**瓶颈已被 profiling 钉死到具体函数与具体原因**,不再是模糊的"太慢"。下一步先做 canon 的 WL 细化(独立卡死),再做 permanent 聚合(独立卡死),最后合体冲 C=4→C=5→C=6。

---

## 🧪 封顶石锻造 #3:WL-canon 单独锻造 → 差分测试当场否决（铁律生效）

### 做法（先建裁判,再锻）
`src/wl_canon.cpp` = **差分测试器**:对大量随机状态,同时跑慢全-$S_{2C}$ canon(已证正确的 oracle)与新 WL-canon,任何不一致即 FAIL。**先验证,后信任。**

### 结果:WL-canon 被差分测试否决(这正是测试的价值)
- C=2:3000 例中 **1322 不一致**;C=3:**2251/3000 不一致** → `WL INSUFFICIENT`。
- 诊断 WL 判别力(`/tmp/wltest`):
  - b=3 混合态:WL 完美细化(6 个单点类)✓
  - **b=2 对称态:WL 坍缩成 1 个类(全 6 符号)** → 仍需满 $6!$ brute,与失败的度数桶无异。
  - WL 是出了名的**在正则/高对称结构上失效**——而我们最难的恰是这种。
- 且高不一致率(~75%)表明我的 WL key 构造**另有逻辑 bug**(列处理仅靠排序,与 slow 的最优符号重标不一致)。

### 精确裁决（诚实,不 ship 半成品）
- **WL 单独不是干净的封顶石**:对一般态有用、对最对称态无用,且我的实现还有 bug。**我不会 ship 一个差分测试未过的 canon。**
- 真正可行的 canon 需要 **WL 细化 + 在残余对称类上做正确的回溯式 individualization-refinement(nauty/traces 的做法)**——这是图同构规范化的标准但**重量级**技术。
- 另一条更对路的思路(回到 permanent):**也许根本不需要规范化**——若把转移权重直接用 permanent 在"符号轨道"上聚合(而非枚举 placement 再 canon),则状态可以是 permanent 能直接消费的**轨道计数向量**,绕开图同构。这更接近 Pettersen/kjellfp 的 a-状态。

### 状态
WL 路线**经差分测试否决**(宝贵的负结果)。封顶石的正确形态收窄为:要么 nauty 级 individualization-refinement,要么 permanent-轨道聚合(更可能是后者,因它本就是 kjellfp 内核)。两者都是真实多会话工程;**绝不 ship 未过差分测试的实现。**

---

## 🧗 封顶石锻造 #4:两个新的、已验证的结构事实(全押 permanent 路)

决定:**只做 permanent 路(B),不两头押注**。路 A(暴力到 2×5)既到不了 2×6 又分心,弃之。

### 新事实 1:stack0(sk) 与骨架顺序无关(已 brute 验证 C=3)
$\text{stack0}(A_0,\dots,A_{C-1})$ 在骨架**任意重排**下不变 ⟹ 只依赖骨架**多重集**,不依赖序列。转移从"序列"降到"多重集",且暗示 stack0 是个对称函数(permanent 候选)。

### 新事实 2:stack0 的精确 Z-模型(已 brute 验证 C=3,4 例全中)
$$\text{stack0}(sk)=\#\{Z:[2C]\times[C]\to\text{columns}\}$$
其中 $Z[s][i]$=符号 s 在第 i 带于 stack0 所在列,约束:
- (a) 每带 i:top 符号 $A_i$ 单射到列、bot 符号 $\bar A_i$ 单射到列(行=双射);
- (b) 每符号 s:其 C 个值 $Z[s][\cdot]$ 互异(s 落在 C 个不同列);
- (c) 每列每符号恰一次(由 (b)+计数自动)。
这是一个 **3-维匹配 / permanent 类计数**,但结构高度规整(每符号度数恰 C)。

### 已排除
- stack0 = top-count 度数谱的函数:✗((1,1,1,2,2,2) 既出 48 又出 72,依赖更细的骨架重叠)。

### 现状(诚实,且方向更聚焦)
- **Solid 新增**:顺序无关性、Z-模型(both brute 验证)。
- 封顶石现在收窄为:把 $N=\sum_{sk}\text{stack0}(sk)^2$ 用 **stack0 的 Z-模型 permanent 表示**,在符号轨道上聚合(避开图同构)。顺序无关性 ⟹ 可按骨架多重集组织,大幅减少枚举。
- 这是真实的下一锤:把 Z-模型写成可快速求值的 permanent/转移,先 C=3 复现 28200960,再 C=4/5/6。**仍需逐级 brute 卡死。**

---

## 🎉🌅 重大发现:正确的轨道状态 = 跨 stack 交集矩阵(豁然开朗)

### 发现(已 brute 验证 C=2→288, C=3→28200960)
联合 (X,Y) 状态的**充分不变量** = **跨 stack 列交集矩阵**
$$T[a][b]=|X_a \cap Y_b|\quad(a,b\in\{0..C-1\})$$
在行置换(X-列重排)× 列置换(Y-列重排)下规范化即可——**只需 $C!^2$**,彻底绕开 WL 撞墙的 $S_{2C}$ 图同构。
- 验证:C=2→288 ✓,C=3→28200960 ✓;均**仅 3 状态/带**(与全 canon 一致)。
- 状态从"指数难规范化的符号集合"→"$C\times C$ 小整数矩阵"。**这是 permanent 能直接消费的形态。**

### 为什么它充分(直觉)
转移只需知道:下一个共享骨架 A 的符号,如何在 X 列与 Y 列中落位而不冲突。这只依赖"哪些 X 列与哪些 Y 列共享多少符号"=交集矩阵 $T$;符号身份无关。顺序无关性(已证)进一步保证多重集足够。

### 现状
- **状态问题已解决**(重大):小矩阵 + $C!^2$ 规范化,已双值验证。
- **转移仍枚举 placement**(Python 版 C=3 用 8.3s 在此)——但这不再是迷雾:下一锤用 permanent 在 $T$ 矩阵上聚合转移权重(不枚举 placement)。
- ⟹ 路径已全程清晰:① 状态=T矩阵 ✅;② permanent 聚合转移(待锻);③ C=3/4 卡死;④ 冲 C=5(OEIS);⑤ 冲 C=6 新值。

---

## 🔨 封顶石 #5:T-矩阵转移已实现+验证,转移良定性已证,瓶颈精确化

### 已落地、已验证(commit 本批)
`src/tmatrix.cpp`:T-矩阵状态($T[a][b]=|X_a\cap Y_b|$,$C!^2$ 规范化)+ 联合转移。
- **C=2→288 ✅,C=3→28200960 ✅,且 C=3 仅 0.1s**(对比旧 M! canon 的 29s,提速 ~290×)。
- 状态数:C=2 是 2,1;C=3 是 3,3,1。极小。

### 关键定理(brute 验证):转移在 T-态上良定
对同一 T-态的**不同具体代表 (X,Y)**,其前向转移分布**完全相同**(已验证 C=3 band-0)。
⟹ 可建纯 **T→T 转移矩阵**,只算一次再反复应用 = 多项式核的根据。

### 瓶颈精确定位(profiling)
C=4 band-0 卡住:`rawTrans = C(8,4)·((4!)^2)^2 ≈ 2300 万` placement 对,每个算 tkey。
- 根因:虽然结果坍缩成极少 T-态,但代码在坍缩**前**枚举了全部 (nx,ny) placement 对。
- 状态问题已解决;**唯一瓶颈 = placement 枚举**(X-place × Y-place 的叉积)。

### 下一锤(清晰,但需小心验证)
把 X-place 与 Y-place **先各自按列内容类坍缩、再做叉积** —— 但要保留计算 T 所需的跨 stack 符号关联(不能简单独立坍缩,T 依赖 $X_a,Y_b$ 实际共享哪些符号)。正确做法:用 permanent 直接算"源 T-态 + 骨架 A → 目标 T-态"的权重。良定性已证 ⟹ 此权重只依赖 T,可一次性算。仍逐级 brute 卡死(C=3→4→5→6)。

---

## 🌉🔑 合龙的拱心石:Δ = 两个置换矩阵之和(已验证)

### 转移的精确结构(brute 验证 C=3)
$T' = T + \Delta$,其中 $\Delta = P_{\pi_A} + P_{\pi_B}$ —— **两个 $C\times C$ 置换矩阵之和**!
- $\pi_A$:本带共享骨架 A 的 top 符号在 X-列与 Y-列间诱导的双射(A-符号在两侧都是 top);
- $\pi_B$:~A 符号(两侧都是 bot)诱导的双射。
- 从空状态:$\text{weight}(\text{empty}\to T') = \binom{2C}{C}\cdot(C!)^2\cdot\#\{(\pi_A,\pi_B):\text{canon}(P_{\pi_A}+P_{\pi_B})=T'\}$。
- 验证:$(\pi_A,\pi_B)$ 计数 = 6,18,12;× $\binom{6}{3}(3!)^2=720$ = 4320,12960,8640 = brute 权重 ✅;总和 25920 ✅。

### 为什么这合龙了桥
- **不再枚举 placement**($(C!)^2$ 的叉积):转移 = 枚举 $(\pi_A,\pi_B)$ 置换对(或非空带时为 permanent),$\Delta$ 是两置换矩阵之和。
- 常数 $\binom{2C}{C}(C!)^2$ 是骨架选择 × 内部符号排序,principled。
- **非空带**:避让约束(符号不能进已含它的列)把"自由置换计数"换成 **permanent**(0/1 允许矩阵的积和式)——这正是 Pettersen 内核的本来面目,现在从我们自己的结构推了出来。

### 状态
拱心石已验证(C=3 band-0)。剩:把非空带的 permanent 形式写全 + C++ 实现 + 逐级卡死 C=3/4/5 → C=6。双线 workflow(数学推完整公式 / CS 建快速实现)进行中。**这是桥的最后一块石头,形状已完全清晰。**

---

## ⚠️ 重大修正:Δ ≠ 两置换矩阵之和(差分测试在非空带抓到隐藏交叉项)

### 我之前的拱心石不完整(诚实纠错)
从**空状态**,$\Delta=P_{\pi_A}+P_{\pi_B}$ 成立(已验证)。但差分测试在**非空带**抓到:权重(4,8,4)对,但 T 值系统性偏移。诊断出根因——
**漏了交叉项**:放入 X-列 a 的新符号,可能**已存在于某个 Y-列 b 的旧内容**里 ⟹ 给 $T[a][b]$ 额外 +1(反向亦然:新 Y-符号撞旧 X-符号)。
所以 $\Delta[a][b]$ 不只是两置换矩阵,还有"新符号 × 旧对侧内容"的耦合项。

### 这意味着什么(关键)
转移**真的依赖具体哪些符号在哪**(不只 T 矩阵的计数)——纯置换对 / 简单 permanent 公式**不完整**。这正是 Pettersen 内核处理的真实难点,比我的拱心石更微妙。
- **好消息**:"转移良定于 T"(早先验证)仍成立 ⟹ 正确的权重确实只依赖 T,但其**计算**需要正确处理交叉项(可能要按符号的"(在哪些X列, 在哪些Y列)"联合 profile 做更细的 permanent / 容斥)。
- **差分测试再次救场**:若不验证就用这个公式冲 C=6,会算出一个**看似精确实则错误**的新值——正是最危险的情形。

### 现状(诚实)
- **Solid 不变**:结构定理、T 状态充分且良定、状态维数小、慢 oracle、空状态权重公式。
- **被修正**:非空带的 Δ 含隐藏交叉项 ⟹ permanent 公式需扩展。这是真实的、定义清晰的下一步,但比预期更微妙,**不会仓促给公式**。
- 下一步:把符号按"(X-列归属, Y-列归属)联合 profile"分类,推导含交叉项的正确权重,brute 卡死非空带,再 C=3→4→5→6。

---

## 🌈 风雨后的彩虹:卷积公式验证通过(C!⁴ → 2·C!²)

### 修正后的正确转移(brute 验证 54/54,含非空带)
$T'=T_{\text{base}}+\Delta_A+\Delta_B$,其中 $\Delta_A$(A=top 符号侧)、$\Delta_B$(~A=bot 侧)**相互独立**。每侧的 $\Delta$ 含全部三类项:
- 新符号 s 同时进 X'列 $a_x$、Y'列 $a_y$ → $\Delta[a_x][a_y]{+}1$;
- 新 X-符号 s 撞旧 $Y_b$ → $\Delta[a_x][b]{+}1$;
- 新 Y-符号 s 撞旧 $X_a$ → $\Delta[a][a_y]{+}1$。
⟹ **权重 = distA ⊛ distB(卷积)**:各侧独立枚举 $(C!)^2$ 个双射得 $\Delta$ 直方图,再卷积。
**把 $(C!)^4$ 降到 $2(C!)^2$ + 卷积**。已 brute 卡死(54 例,含非空带交叉项)。

### 意义
- 之前的"漏交叉项"bug 已修正,且修正后**仍然因式分解**(A 侧 ⊥ ~A 侧),只是每侧的 $\Delta$ 要含交叉项。
- 成本:C=3 秒级;C=4 从 ~10¹¹ 降到可行;C=5/6 取决于 distA/distB 的实际大小(Δ 元素是小整数,直方图远小于 $(C!^2)^2$)。
- **这是把封顶石真正凿动的一锤**:转移不再枚举 placement 叉积,而是两个小直方图的卷积。

### 下一步
C++ 实现:逐带,对每个 T-态 × 每个骨架,算 distA/distB(各 $(C!)^2$)、卷积、累加到目标 T-态。逐级卡死 C=3→4→5→6。骨架可按类聚合再省。**仍每级 brute 卡死,绝不跳验证。**

---

## 🧭 诚实评估:封顶石的真实剩余难度(站在 Clarke 商图理论上)

### 用户提供的资料评判
- **Clarke 1993 商转移图**(最对路):严格证明"在轨道上聚合转移是精确的" = 印证我们已证的"转移良定于 T"。**理论背书完整。**
- McKay 典范增广 / orderly generation:为"海量状态免同构枚举"设计;我们状态极少(下表),非瓶颈所在,用不上其剪枝威力。
- 群表示分块对角化:②的重型版,状态太少不需要。

### 已测的硬数据
- **canonical T-状态数极少**:C=2 共 4 个,C=3 共 8 个(逐带 [2,1] / [3,3,1])。
- **卷积权重转移**:已实现、已验证(C=3 逐带金标准全中)、且快。
- **唯一瓶颈**:发现这极少数状态、并为每个取一个代表 (X,Y),目前用 placement 叉积($C!^2\times C!^2$/态/骨架)→ C=4 慢。

### 关键认识(自我修正)
- "从 T 矩阵直接构造代表"**不可行**:符号跨多带出现在多个列,T 是真集合交,非传输边际可还原。
- 正确加速 = **全局缓存 + 商转移矩阵**:distinct 状态总数小,每个状态的出分布只算一次、缓存、跨带复用。但"首次发现 + 算出分布"仍需一次较重的枚举。
- 这一步**可行但需仔细工程**:要么(a)把代表发现也用卷积式 O(poly) 构造(需解决集合实现问题),要么(b)接受"每个 distinct 状态一次性重枚举"(状态少,或许可承受到 C=4/5)。

### 诚实裁决
- **数学已完成**:结构、T-状态、含交叉项卷积转移、良定性、商图理论依据——全部 solid 且验证。
- **剩余 = 一个具体的状态发现/代表构造的高效实现**,它不碰已验证的正确性(任何错误都会被 C=3 金标准与 C=4/5 的 OEIS 值当场抓住)。
- 这是真实的、定义清晰的工程收尾,但**不是一锤的事**;且我已多次在仓促实现时引入低级 bug(被验证链挡下)。**宁可如实标注"工程收尾待续",不假报登顶。**

---

## 🔑🌅 瓶颈彻底溶解:单 stack 唯一 + 对齐生成

### 决定性观察(已验证 C=2,3)
**单个 stack 在每带后只有 1 个 canonical 状态**(C=2,3 实测每带都是 1)。直觉:单 stack = "C 列各含 2b 个符号、合法",彼此全对称。
⟹ X-side 与 Y-side 各处于**唯一** canonical 态。**联合 T-状态完全由 X 列与 Y 列如何对齐(交集)决定**。

### 由此得到的代表生成(绕开 placement 叉积)
1. 直接构造一个 canonical 单-stack 代表 X(poly 时间,放 b 个骨架即可);
2. **所有联合 T-状态 = { canonT(X, relabel_σ(X)) : σ ∈ S_{2C} }** —— 把 Y 取成 X 的符号重标,遍历对齐。
3. 验证:C=3 band-1 由此生成的 3 个 T 恰好 = golden 3 个 ✅。
- 成本 $(2C)!$(对齐枚举)≪ $(C!)^4$(placement 叉积)。且给出每个状态的**具体代表 (X,Y)**,直接喂卷积 sideDist。

### 意义
代表发现从"$(C!)^4$ placement 叉积"降到"$(2C)!$ 对齐枚举",且单 stack 代表是直接构造的。这溶解了 C=4 的瓶颈。配合已验证的卷积权重转移 + 全局状态缓存,C=4→5→6 的引擎齐了。
- 仍逐级 brute 卡死:C=3 金标准 → C=4=29136487207403520 → C=5=OEIS → C=6 新值。

---

## 🔧 C++ 工程收尾:坑已识别+量化,瓶颈收窄到"代表发现"一处

### 专家级排坑(已逐一处理/量化)
1. **u128 溢出**:N(2×6)≈10⁵⁰ > u128(3.4e38)。✅ 已写 `src/big.hpp`(base-1e9 bignum),**单元测试通过**(30! 精确、999999999²、fromDec 往返)。权重全程用 Big。
2. **canonT key 用 char**:T 元素 ≤2C≤12,char 安全;比较用统一序。✅
3. **代表发现成本**:这是**唯一未解瓶颈**(下)。
4. **验证链断裂**:每次改完必跑 C=2/C=3 金标准。✅ 全程保持(288、28200960 逐带）。

### 量化的性能事实(C++ profiling)
- **卷积权重转移**:ONE state @ C=4 = **0.11s**(576 canonT 调用)。**快,不是瓶颈。**
- **单 stack 唯一**:每带 1 个 canonical(已证)→ 联合态由对齐决定。
- **`discover` via (2C)! 对齐**:C=4 = 8.5s / 40320 对齐(仅得 3 态),C=5=10! 约 13min,C=6=12! 不可行。
- **`discover` via placement 叉积**:C=4 band0→1 = 5×70×576×576 keyXY ≈ 10⁸,**band1 35s 未达 → 这是当前 C=4 卡点**。

### 精确诊断
- 权重快、状态少、数值已用 bignum 兜住。**唯一卡点 = 给每个(少数)目标 canonical 态找一个代表**,两种现法(对齐枚举 / placement 叉积)都在 C=4 偏慢。
- 已验证的备选:代表可作 placement 的"首个命中"副产品(O(1)/目标),但需把它从权重叉积里干净剥离,且只对"权重已产生的目标"构造——避免枚举全 placement。这是 clean 的下一步,纯实现,正确性由 C=3 金标准 + C=4 OEIS 值双重卡死。

### 诚实状态
数学 + 数值(bignum)+ 卷积转移 + 状态结构全部 solid 且验证;**C=4 closure 卡在"代表发现"这一处实现效率**,已精确定位、有明确解法(代表作权重副产品 / 对齐枚举加 degree 剪枝)。非数学难点,是最后一段实现打磨。
