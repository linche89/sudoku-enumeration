# Reference Material

This directory contains immutable external papers, source snapshots, OEIS/forum
captures, and FJ05 verification fixtures. Project code must not silently edit
these files.

The authoritative FJ9 fixtures used by the full gate are `jobs2.txt` and
`results2.txt`.
