# Legacy Source Experiments

These files record superseded state models, canonicalizers, profilers, and
oracles from the OG-2 search. They are excluded from standard builds.

Historical documents may refer to their former `src/` paths. Use Git history
or this directory when reproducing those experiments. Do not promote a legacy
program back into the main build without an exact differential gate.
